<UserControl x:Class="PlatformSettings.SharedParams.SharedParamView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PlatformSettings.SharedParams"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             x:Name="_this">
    <StackPanel>
        <TextBox DataContext="{Binding ElementName=_this}" Text="{Binding Path=ViewModel.Name}"/>
        <TextBlock DataContext="{Binding ElementName=_this}" Text="{Binding Path=ViewModel.SharedParamType}" IsEnabled="False"/>
    </StackPanel>
</UserControl>
