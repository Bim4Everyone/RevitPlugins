<Window x:Class="RevitBatchPrint.Views.BatchPrintWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RevitBatchPrint"
             xmlns:views="clr-namespace:RevitBatchPrint.Views"
             xmlns:viewModels="clr-namespace:RevitBatchPrint.ViewModels"
             mc:Ignorable="d" Height="580" Width="850" WindowStartupLocation="CenterOwner" Title="Пакетная печать">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="45"/>
        </Grid.RowDefinitions>

        <StackPanel  Grid.Row="0" Visibility="{Binding ShowPrintParamSelect}">
            <TextBlock Text="Параметр комплекта чертежей" Margin="10,10,10,0"/>
            <ComboBox Margin="10"
                      ItemsSource="{Binding Path=PrintParamNames}"
                      SelectedItem="{Binding Path=PrintParamName}"/>
        </StackPanel>

        <StackPanel  Grid.Row="1">
            <TextBlock Text="Комплект чертежей" Margin="10,10,10,0"/>
            <ComboBox Margin="10" 
                      ItemsSource="{Binding Path=Albums}"
                      SelectedItem="{Binding Path=SelectedAlbum}"/>
        </StackPanel>

        <GroupBox Grid.Row="2" Header="Настройки принтера">
            <views:PrintSettingsView DataContext="{Binding PrintSettings}" />
        </GroupBox>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <TextBlock Margin="10" Text="{Binding ErrorText}" Foreground="Orange" />
            <Button Content="ОК" Margin="10" Click="ButtonOk_Click" Width="80" Command="{Binding RevitPrintCommand}" />
            <Button Content="Отмена" Margin="10" Click="ButtonCancel_Click" Width="80"/>
        </StackPanel>
    </Grid>
</Window>
