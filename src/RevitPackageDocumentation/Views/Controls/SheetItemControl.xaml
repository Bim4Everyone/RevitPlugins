<UserControl
    x:Class="RevitPackageDocumentation.Views.Controls.SheetItemControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:RevitPackageDocumentation.Views.Controls"
    xmlns:sheet="clr-namespace:RevitPackageDocumentation.ViewModels.Configuration.Sheet"
    xmlns:converters="clr-namespace:dosymep.WPF.Converters"
    
    d:DataContext="{d:DesignInstance Type=sheet:SheetVM}"
    mc:Ignorable="d"
    d:DesignHeight="100"
    d:DesignWidth="300">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary
                    Source="../CustomStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <converters:BoolToVisibilityConverter
                x:Key="BoolToVisibilityConverter" />
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <ui:TextBox
            Grid.Column="0"
            Margin="5"
            Text="{Binding Path=ModuleName, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
            VerticalContentAlignment="Center"
            HorizontalAlignment="Stretch" />

        <ui:TextBlock
            Grid.Column="1"
            Margin="5"
            Text="!"
            FontSize="26"
            FontWeight="Bold"
            Foreground="Orange"
            VerticalAlignment="Center"
            HorizontalAlignment="Right"
            Visibility="{Binding Path=HasErrors, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay, 
                Converter={StaticResource BoolToVisibilityConverter}}"/>

        <CheckBox
            Grid.Column="2"
            Margin="-12 -2.5 12 0"
            VerticalAlignment="Top"
            IsChecked="{Binding Path=IsModuleCheck, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}">
            <CheckBox.RenderTransform>
                <ScaleTransform
                    ScaleX="1.55"
                    ScaleY="1.55" />
            </CheckBox.RenderTransform>
        </CheckBox>

        <ui:Button
            Grid.Column="3"
            Margin="5"
            Height="35"
            Width="35"
            Padding="0"
            VerticalAlignment="Center"
            Icon="{ui:SymbolIcon Flash28}"
            Command="{Binding Path=CreateSheetCommand}">
        </ui:Button>

        <ui:Button
            Grid.Column="4"
            Margin="5"
            Height="35"
            Width="35"
            VerticalAlignment="Center"
            Appearance="Danger"
            Content="✕"
            Command="{Binding 
                Path=DataContext.CurrentSheetSet.RemoveSheetCommand, 
                RelativeSource={RelativeSource AncestorType=ItemsControl}}"
            CommandParameter="{Binding}">
        </ui:Button>
    </Grid>
</UserControl>