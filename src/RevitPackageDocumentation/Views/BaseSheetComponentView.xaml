<UserControl
    x:Class="RevitPackageDocumentation.Views.BaseSheetComponentView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:local="clr-namespace:RevitPackageDocumentation.Views"
    mc:Ignorable="d"
    x:Name="root"
    d:DesignHeight="450"
    d:DesignWidth="800">
    <Border
        BorderBrush="Gray"
        BorderThickness="1"
        Margin="5"
        Padding="5">
        <DockPanel>
            <TextBlock
                DockPanel.Dock="Top"
                Text="{Binding ModuleName}"
                FontWeight="Bold"
                FontSize="14"
                Margin="0,0,0,5" />
            <ContentControl
                Content="{Binding}"
                ContentTemplate="{Binding DerivedTemplate, ElementName=root}" />
        </DockPanel>
    </Border>
</UserControl>