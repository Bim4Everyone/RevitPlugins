<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:c="clr-namespace:Wpf.Ui.Controls;assembly=Wpf.Ui">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary
            Source="pack://application:,,,/Wpf.Ui;component/Controls/ComboBox/ComboBox.xaml" />
        <ResourceDictionary
            Source="pack://application:,,,/Wpf.Ui;component/Controls/TextBox/TextBox.xaml" />
        <ResourceDictionary
            Source="pack://application:,,,/Wpf.Ui;component/Controls/NumberBox/NumberBox.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <SolidColorBrush
        x:Key="ErrorBackgroundBrush"
        Color="Orange"
        Opacity="0.1" />
    <SolidColorBrush
        x:Key="ErrorBorderBrush"
        Color="Orange" />
    <Thickness
        x:Key="ErrorBorderThickness">1</Thickness>

    <Style
        x:Key="TextBoxForError"
        TargetType="ui:TextBox"
        BasedOn="{StaticResource DefaultTextBoxStyle}">
        <Setter
            Property="Visibility"
            Value="Visible" />
        <Setter
            Property="Opacity"
            Value="1" />
        <Style.Triggers>
            <DataTrigger
                Binding="{Binding Tag, RelativeSource={RelativeSource Self}}"
                Value="">
                <DataTrigger.EnterActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation
                                Storyboard.TargetProperty="Opacity"
                                To="0"
                                Duration="0:0:0.3"
                                FillBehavior="HoldEnd" />
                            <ObjectAnimationUsingKeyFrames
                                Storyboard.TargetProperty="Visibility"
                                BeginTime="0:0:0.3">
                                <DiscreteObjectKeyFrame
                                    KeyTime="0:0:0.3">
                                    <DiscreteObjectKeyFrame.Value>
                                        <Visibility>Collapsed</Visibility>
                                    </DiscreteObjectKeyFrame.Value>
                                </DiscreteObjectKeyFrame>
                            </ObjectAnimationUsingKeyFrames>
                        </Storyboard>
                    </BeginStoryboard>
                </DataTrigger.EnterActions>
                <DataTrigger.ExitActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <ObjectAnimationUsingKeyFrames
                                Storyboard.TargetProperty="Visibility"
                                BeginTime="0:0:0">
                                <DiscreteObjectKeyFrame
                                    KeyTime="0:0:0">
                                    <DiscreteObjectKeyFrame.Value>
                                        <Visibility>Visible</Visibility>
                                    </DiscreteObjectKeyFrame.Value>
                                </DiscreteObjectKeyFrame>
                            </ObjectAnimationUsingKeyFrames>
                            <DoubleAnimation
                                Storyboard.TargetProperty="Opacity"
                                To="1"
                                Duration="0:0:0.3"
                                BeginTime="0:0:0" />
                        </Storyboard>
                    </BeginStoryboard>
                </DataTrigger.ExitActions>
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <Style
        x:Key="ErrorTextBoxStyle"
        TargetType="ui:TextBox"
        BasedOn="{StaticResource DefaultTextBoxStyle}">

        <Setter
            Property="Validation.ErrorTemplate"
            Value="{x:Null}" />
        <Setter
            Property="IsEnabled"
            Value="True" />

        <Style.Triggers>
            <Trigger
                Property="Validation.HasError"
                Value="True">
                <Setter
                    Property="BorderBrush"
                    Value="{StaticResource ErrorBorderBrush}" />

                <Setter
                    Property="BorderThickness"
                    Value="{StaticResource ErrorBorderThickness}" />

                <Setter
                    Property="Background"
                    Value="{StaticResource ErrorBackgroundBrush}" />
            </Trigger>

            <DataTrigger
                Binding="{Binding DataContext.CurrentSheetSet.SelectedSheet, RelativeSource={RelativeSource AncestorType=Window}}"
                Value="{x:Null}">
                <Setter
                    Property="IsEnabled"
                    Value="False" />
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <Style
        x:Key="ErrorControlTextBoxStyle"
        TargetType="ui:TextBox"
        BasedOn="{StaticResource DefaultTextBoxStyle}">

        <Setter
            Property="IsEnabled"
            Value="True" />
        
        <Style.Triggers>
            <DataTrigger
                Binding="{Binding
                    RelativeSource={RelativeSource AncestorType={x:Type UserControl}},
                    Path=(Validation.HasError)}"
                Value="True">
                <Setter
                    Property="BorderBrush"
                    Value="{StaticResource ErrorBorderBrush}" />

                <Setter
                    Property="BorderThickness"
                    Value="{StaticResource ErrorBorderThickness}" />

                <Setter
                    Property="Background"
                    Value="{StaticResource ErrorBackgroundBrush}"/>
            </DataTrigger>

            <DataTrigger
                Binding="{Binding DataContext.CurrentSheetSet.SelectedSheet, RelativeSource={RelativeSource AncestorType=Window}}"
                Value="{x:Null}">
                <Setter
                    Property="IsEnabled"
                    Value="False" />
            </DataTrigger>
        </Style.Triggers>
    </Style>


    <Style
        x:Key="ErrorComboBoxStyle"
        TargetType="ComboBox"
        BasedOn="{StaticResource DefaultComboBoxStyle}">

        <Setter
            Property="Validation.ErrorTemplate"
            Value="{x:Null}" />
        <Setter
            Property="IsEnabled"
            Value="True" />

        <Style.Triggers>
            <Trigger
                Property="Validation.HasError"
                Value="True">
                <Setter
                    Property="BorderBrush"
                    Value="{StaticResource ErrorBorderBrush}" />

                <Setter
                    Property="BorderThickness"
                    Value="{StaticResource ErrorBorderThickness}" />

                <Setter
                    Property="Background"
                    Value="{StaticResource ErrorBackgroundBrush}" />
            </Trigger>

            <DataTrigger
                Binding="{Binding DataContext.CurrentSheetSet.SelectedSheet, RelativeSource={RelativeSource AncestorType=Window}}"
                Value="{x:Null}">
                <Setter
                    Property="IsEnabled"
                    Value="False" />
            </DataTrigger>
        </Style.Triggers>
    </Style>

    
    <Style
        x:Key="ErrorControlComboBoxStyle"
        TargetType="ComboBox"
        BasedOn="{StaticResource DefaultComboBoxStyle}">

        <Setter
            Property="Validation.ErrorTemplate"
            Value="{x:Null}" />
        <Setter
            Property="IsEnabled"
            Value="True" />

        <Style.Triggers>
            <DataTrigger
                Binding="{Binding
                    RelativeSource={RelativeSource AncestorType={x:Type UserControl}},
                    Path=(Validation.HasError)}"
                Value="True">
                <Setter
                    Property="BorderBrush"
                    Value="{StaticResource ErrorBorderBrush}" />

                <Setter
                    Property="BorderThickness"
                    Value="{StaticResource ErrorBorderThickness}" />

                <Setter
                    Property="Background"
                    Value="{StaticResource ErrorBackgroundBrush}" />
            </DataTrigger>

            <DataTrigger
                Binding="{Binding DataContext.CurrentSheetSet.SelectedSheet, RelativeSource={RelativeSource AncestorType=Window}}"
                Value="{x:Null}">
                <Setter
                    Property="IsEnabled"
                    Value="False" />
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <Style
        x:Key="ErrorNumberBoxStyle"
        TargetType="ui:NumberBox"
        BasedOn="{StaticResource DefaultUiNumberBoxStyle}">

        <Setter
            Property="Validation.ErrorTemplate"
            Value="{x:Null}" />
        <Setter
            Property="IsEnabled"
            Value="True" />

        <Style.Triggers>
            <Trigger
                Property="Validation.HasError"
                Value="True">
                <Setter
                    Property="BorderBrush"
                    Value="{StaticResource ErrorBorderBrush}" />

                <Setter
                    Property="BorderThickness"
                    Value="{StaticResource ErrorBorderThickness}" />

                <Setter
                    Property="Background"
                    Value="{StaticResource ErrorBackgroundBrush}" />
            </Trigger>

            <DataTrigger
                Binding="{Binding DataContext.CurrentSheetSet.SelectedSheet, RelativeSource={RelativeSource AncestorType=Window}}"
                Value="{x:Null}">
                <Setter
                    Property="IsEnabled"
                    Value="False" />
            </DataTrigger>
        </Style.Triggers>
    </Style>
</ResourceDictionary>