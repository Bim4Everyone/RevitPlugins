<UserControl
    x:Class="RevitPylonDocumentation.Views.Controls.SettingsTitleControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    x:Name="_this"
    HorizontalAlignment="Stretch"
    mc:Ignorable="d"
    d:DesignHeight="100"
    d:DesignWidth="480">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary
                    Source="../CustomStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
    
        <ui:TextBlock
            Grid.Column="0"
            Margin="5"
            FontSize="16"
            Text="{Binding LabelText, ElementName=_this}"
            Style="{StaticResource TextBlockWithTooltipStyle}"
            ToolTip="{Binding ToolTipText, ElementName=_this}" />

        <ui:Button
            Grid.Column="1"
            Margin="5"
            Padding="5"
            Icon="{ui:SymbolIcon Save28}"
            Appearance="Info"
            Command="{Binding ButtonCommand, ElementName=_this}">
            <i:Interaction.Triggers>
                <i:EventTrigger
                    EventName="MouseLeave">
                    <i:InvokeCommandAction
                        Command="{Binding ButtonMouseLeaveCommand, ElementName=_this}" />
                </i:EventTrigger>
            </i:Interaction.Triggers>
        </ui:Button>
    </Grid>
</UserControl>
