# RevitAreaBoundaries (Границы зон ТЭП)
Плагин для автоматического определения внешних границ здания в пределах выбранного плана зонирования

# Сборка проекта
```
nuke compile --profile RevitAreaBoundaries
```

# Публикация проекта
```
nuke publish --profile RevitAreaBoundaries
```
