<core:WpfUIPlatformWindow
    x:Class="RevitBatchSpecExport.Views.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:vms="clr-namespace:RevitBatchSpecExport.ViewModels"
    xmlns:core="clr-namespace:dosymep.WpfUI.Core"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:me="clr-namespace:dosymep.WpfCore.MarkupExtensions;assembly=dosymep.WpfCore"
    xmlns:wpfbehaviors="clr-namespace:dosymep.WpfCore.Behaviors;assembly=dosymep.WpfCore"
    mc:Ignorable="d"
    WindowStartupLocation="CenterOwner"
    d:DesignHeight="600"
    d:DesignWidth="600"
    Height="450"
    Width="500"
    MinHeight="400"
    MinWidth="400"
    d:DataContext="{d:DesignInstance vms:MainViewModel, IsDesignTimeCreatable=False}">
    <i:Interaction.Triggers>
        <i:EventTrigger
            EventName="Loaded">
            <i:InvokeCommandAction
                Command="{Binding LoadViewCommand}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>
    <core:WpfUIPlatformWindow.Resources>
        <Style
            TargetType="Border"
            x:Key="CustomBorder">
            <Setter
                Property="Padding"
                Value="20" />
            <Setter
                Property="VerticalAlignment"
                Value="Stretch" />
            <Setter
                Property="HorizontalAlignment"
                Value="Stretch" />
            <Setter
                Property="SnapsToDevicePixels"
                Value="True" />
            <Setter
                Property="OverridesDefaultStyle"
                Value="True" />
            <Setter
                Property="ScrollViewer.HorizontalScrollBarVisibility"
                Value="Auto" />
            <Setter
                Property="ScrollViewer.VerticalScrollBarVisibility"
                Value="Disabled" />
            <Setter
                Property="Background"
                Value="{DynamicResource CardBackground}" />
            <Setter
                Property="BorderBrush"
                Value="{DynamicResource CardBorderBrush}" />
            <Setter
                Property="BorderThickness"
                Value="{DynamicResource CardControlBorderThemeThickness}" />
            <Setter
                Property="FocusVisualStyle"
                Value="{DynamicResource DefaultControlFocusVisualStyle}" />
            <Setter
                Property="CornerRadius"
                Value="{DynamicResource ControlCornerRadius}" />
        </Style>
    </core:WpfUIPlatformWindow.Resources>
    <b:Interaction.Behaviors>
        <wpfbehaviors:WpfAttachServiceBehavior
            AttachableService="{Binding ProgressDialogFactory}" />
        <wpfbehaviors:WpfAttachServiceBehavior
            AttachableService="{Binding OpenFileDialogService}" />
        <wpfbehaviors:WpfAttachServiceBehavior
            AttachableService="{Binding OpenFolderDialogService}" />
        <wpfbehaviors:WpfAttachServiceBehavior
            AttachableService="{Binding MessageBoxService}" />
    </b:Interaction.Behaviors>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition
                Height="Auto" />
            <RowDefinition
                Height="Auto" />
            <RowDefinition
                Height="*" />
            <RowDefinition
                Height="Auto" />
        </Grid.RowDefinitions>

        <ui:TitleBar
            Grid.Row="0"
            Title="{me:LocalizationSource MainWindow.Title}" />

        <Border
            Grid.Row="1"
            Margin="10"
            Style="{StaticResource CustomBorder}">
            <DockPanel
                LastChildFill="True">
                <Label
                    Margin="0 0 10 0"
                    VerticalContentAlignment="Center"
                    Content="{me:LocalizationSource MainWindow.SelectedParameter}" />
                <ui:TextBox
                    ToolTip="{Binding ParamName}"
                    Text="{Binding ParamName, UpdateSourceTrigger=PropertyChanged}" />
            </DockPanel>
        </Border>

        <Border
            Grid.Row="2"
            Margin="10 0"
            Style="{StaticResource CustomBorder}">
            <DockPanel
                LastChildFill="True">
                <Label
                    Margin="0 0 0 10"
                    DockPanel.Dock="Top"
                    Content="{me:LocalizationSource MainWindow.SelectedProjects}" />
                <ui:ListView
                    ItemsSource="{Binding Documents}"
                    SelectedItem="{Binding SelectedDocument}" />
            </DockPanel>
        </Border>

        <StackPanel
            Grid.Row="3"
            Orientation="Horizontal"
            HorizontalAlignment="Right">
            <TextBlock
                Margin="10"
                Foreground="Orange"
                VerticalAlignment="Center"
                Text="{Binding ErrorText}" />

            <ui:Button
                Margin="10"
                Appearance="Danger"
                Content="{ui:SymbolIcon Symbol=Delete24}"
                CommandParameter="{Binding SelectedDocument}"
                Command="{Binding RemoveDocumentCommand}" />

            <ui:Button
                Margin="10"
                Content="{ui:SymbolIcon Symbol=Add24}"
                Command="{Binding AddDocumentCommand}" />

            <ui:Button
                Margin="10"
                Width="80"
                Appearance="Info"
                IsDefault="True"
                Click="ButtonOk_Click"
                Content="{me:LocalizationSource Ok}"
                Command="{Binding ProcessDocumentsCommand}" />

            <ui:Button
                Margin="10"
                Width="80"
                IsCancel="True"
                Click="ButtonCancel_Click"
                Content="{me:LocalizationSource Cancel}" />
        </StackPanel>
    </Grid>
</core:WpfUIPlatformWindow>