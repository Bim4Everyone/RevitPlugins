<core:WpfUIPlatformWindow
    x:Class="RevitClashDetective.Views.Common.EntityNameView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:RevitClashDetective.ViewModels.Common"
    xmlns:core="clr-namespace:dosymep.WpfUI.Core"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:me="clr-namespace:dosymep.WpfCore.MarkupExtensions;assembly=dosymep.WpfCore"
    mc:Ignorable="d"
    WindowStartupLocation="CenterOwner"
    Height="300"
    Width="600"
    MinHeight="300"
    MinWidth="600"
    MaxHeight="300"
    MaxWidth="600"
    ResizeMode="NoResize"
    d:DataContext="{d:DesignInstance vm:EntityNameViewModel, IsDesignTimeCreatable=False}">
    <core:WpfUIPlatformWindow.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary
                    Source="../../Resources/CustomStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </core:WpfUIPlatformWindow.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition
                Height="Auto" />
            <RowDefinition
                Height="*" />
            <RowDefinition
                Height="Auto" />
        </Grid.RowDefinitions>

        <ui:TitleBar
            Grid.Row="0"
            ShowMinimize="False"
            ShowMaximize="False"
            Title="{me:LocalizationSource EntityNameView.Title}" />

        <StackPanel
            Grid.Row="1"
            VerticalAlignment="Center">
            <Label
                HorizontalAlignment="Center"
                Style="{StaticResource CustomLabel}"
                Content="{me:LocalizationSource EntityNameView.Label}" />
            <ui:TextBox
                Width="450"
                MinWidth="450"
                MaxWidth="450"
                VerticalContentAlignment="Center"
                Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" />
        </StackPanel>

        <StackPanel
            Grid.Row="2"
            Orientation="Horizontal"
            HorizontalAlignment="Right">

            <ui:TextBlock
                Margin="10"
                Text="{Binding ErrorText}"
                Style="{StaticResource CustomErrorText}" />

            <ui:Button
                Width="80"
                Margin="10"
                IsDefault="True"
                Appearance="Info"
                Click="ButtonOk_Click"
                Content="{me:LocalizationSource Ok}"
                Command="{Binding AcceptCommand}" />

            <ui:Button
                Width="80"
                Margin="10"
                IsCancel="True"
                Click="ButtonCancel_Click"
                Content="{me:LocalizationSource Cancel}" />
        </StackPanel>
    </Grid>
</core:WpfUIPlatformWindow>
