<core:WpfUIPlatformWindow
    x:Class="RevitClashDetective.Views.FilterCreatorView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:local="clr-namespace:RevitClashDetective.Views"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:RevitClashDetective.ViewModels.FilterCreatorViewModels"
    xmlns:core="clr-namespace:dosymep.WpfUI.Core"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:me="clr-namespace:dosymep.WpfCore.MarkupExtensions;assembly=dosymep.WpfCore"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:wpfbehaviors="clr-namespace:dosymep.WpfCore.Behaviors;assembly=dosymep.WpfCore"
    mc:Ignorable="d"
    Height="600"
    Width="1000"
    MinHeight="500"
    MinWidth="850"
    d:DataContext="{d:DesignInstance vm:FiltersViewModel, IsDesignTimeCreatable=False}">

    <i:Interaction.Behaviors>
        <wpfbehaviors:WpfAttachServiceBehavior
            AttachableService="{Binding MessageBoxService}" />
        <wpfbehaviors:WpfAttachServiceBehavior
            AttachableService="{Binding OpenFileDialogService}" />
        <wpfbehaviors:WpfAttachServiceBehavior
            AttachableService="{Binding SaveFileDialogService}" />
    </i:Interaction.Behaviors>
    <i:Interaction.Triggers>
        <i:EventTrigger
            EventName="Closing">
            <i:InvokeCommandAction
                Command="{Binding AskForSaveCommand}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>

    <core:WpfUIPlatformWindow.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary
                    Source="../Resources/CustomStyles.xaml" />
                <ResourceDictionary
                    Source="pack://application:,,,/Wpf.Ui;component/Controls/ListView/ListView.xaml" />
                <ResourceDictionary
                    Source="pack://application:,,,/Wpf.Ui;component/Controls/ListView/ListViewItem.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </core:WpfUIPlatformWindow.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition
                Height="Auto" />
            <RowDefinition
                Height="Auto" />
            <RowDefinition
                Height="*" />
            <RowDefinition
                Height="Auto" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition
                Width="1.5*"
                MinWidth="200" />
            <ColumnDefinition
                Width="Auto" />
            <ColumnDefinition
                Width="2*"
                MinWidth="200" />
            <ColumnDefinition
                Width="Auto" />
            <ColumnDefinition
                Width="4*"
                MinWidth="200" />
        </Grid.ColumnDefinitions>

        <ui:TitleBar
            Grid.Row="0"
            Grid.Column="0"
            Grid.ColumnSpan="5"
            Title="{me:LocalizationSource FilterCreation.Title}" />

        <Border
            Grid.Row="1"
            Grid.Column="0"
            Grid.ColumnSpan="5"
            Margin="8 0"
            Style="{StaticResource CustomBorder}">
            <DockPanel>
                <ui:Button
                    ToolTip="{me:LocalizationSource FilterCreation.Load}"
                    Command="{Binding LoadCommand}"
                    Content="{ui:SymbolIcon Symbol=ArrowDownload20}" />
                <ui:Button
                    Margin="8 0"
                    ToolTip="{me:LocalizationSource FilterCreation.Save}"
                    Command="{Binding SaveCommand}"
                    Content="{ui:SymbolIcon Symbol=Save20}" />
                <ui:Button
                    ToolTip="{me:LocalizationSource FilterCreation.SaveAs}"
                    Command="{Binding SaveAsCommand}"
                    Content="{ui:SymbolIcon Symbol=SaveEdit20}" />
            </DockPanel>
        </Border>

        <Border
            Grid.Row="2"
            Grid.Column="0"
            Margin="8 0 0 0"
            Style="{StaticResource CustomBorder}">
            <DockPanel
                LastChildFill="True">
                <DockPanel
                    DockPanel.Dock="Top">
                    <Label
                        DockPanel.Dock="Top"
                        Margin="0 0 0 4"
                        VerticalAlignment="Center"
                        Content="{me:LocalizationSource FilterCreation.SearchSets}" />
                    <ui:Button
                        ToolTip="{me:LocalizationSource FilterCreation.AddSearchSet}"
                        Command="{Binding CreateCommand}"
                        Content="{ui:SymbolIcon Symbol=Add20}" />
                    <ui:Button
                        Margin="4 0"
                        ToolTip="{me:LocalizationSource FilterCreation.RenameSearchSet}"
                        Command="{Binding RenameCommand}"
                        Content="{ui:SymbolIcon Symbol=Rename20}" />
                    <ui:Button
                        ToolTip="{me:LocalizationSource FilterCreation.CopySearchSet}"
                        Command="{Binding CopyCommand}"
                        Content="{ui:SymbolIcon Symbol=Copy20}" />
                    <ui:Button
                        Appearance="Danger"
                        Margin="4 0 0 0"
                        ToolTip="{me:LocalizationSource FilterCreation.DeleteSearchSet}"
                        Command="{Binding DeleteCommand}"
                        CommandParameter="{Binding Path=SelectedItems, ElementName=_filtersList}"
                        Content="{ui:SymbolIcon Symbol=Delete20}" />
                </DockPanel>
                <ui:ListView
                    x:Name="_filtersList"
                    Margin="0 4 0 0"
                    SelectionMode="Extended"
                    ItemsSource="{Binding Filters}"
                    SelectedItem="{Binding SelectedFilter, UpdateSourceTrigger=PropertyChanged}">
                    <ui:ListView.ItemTemplate>
                        <DataTemplate>
                            <DockPanel
                                Height="20"
                                Margin="10 4"
                                VerticalAlignment="Center">
                                <ui:TextBlock
                                    VerticalAlignment="Center"
                                    Text="{Binding Name}" />
                            </DockPanel>
                        </DataTemplate>
                    </ui:ListView.ItemTemplate>
                </ui:ListView>
            </DockPanel>
        </Border>

        <GridSplitter
            Grid.Row="2"
            Grid.Column="1"
            Style="{StaticResource CustomGridSplitter}" />

        <Border
            Grid.Row="2"
            Grid.Column="2"
            Style="{StaticResource CustomBorder}">
            <DockPanel
                LastChildFill="True">
                <Label
                    DockPanel.Dock="Top"
                    Content="{me:LocalizationSource FilterCreation.Categories}" />
                <local:CategoryView
                    DataContext="{Binding SelectedFilter}" />
            </DockPanel>
        </Border>

        <GridSplitter
            Grid.Row="2"
            Grid.Column="3"
            Style="{StaticResource CustomGridSplitter}" />

        <Border
            Grid.Row="2"
            Grid.Column="4"
            Margin="0 0 8 0"
            Style="{StaticResource CustomBorder}">
            <DockPanel
                LastChildFill="True">
                <Label
                    DockPanel.Dock="Top"
                    Content="{me:LocalizationSource FilterCreation.Criteria}" />
                <ScrollViewer>
                    <local:CriterionView
                        DataContext="{Binding SelectedFilter.Set}" />
                </ScrollViewer>
            </DockPanel>
        </Border>

        <StackPanel
            Grid.Column="0"
            Grid.Row="3"
            Grid.ColumnSpan="5"
            Orientation="Horizontal"
            HorizontalAlignment="Right">
            <ui:TextBlock
                Margin="8"
                Text="{Binding ErrorText}"
                Style="{StaticResource CustomErrorText}" />
            <ui:TextBlock
                Margin="8"
                Text="{Binding MessageText}"
                Style="{StaticResource CustomMessageText}" />
            <ui:Button
                Margin="8"
                Click="ButtonOk_Click"
                ToolTip="{me:LocalizationSource FilterCreation.CheckSetButtonTooltip}"
                Content="{me:LocalizationSource FilterCreation.CheckSetButton}"
                Command="{Binding CheckSearchSetCommand}" />
            <ui:Button
                Margin="8"
                Width="80"
                Appearance="Info"
                IsDefault="True"
                Click="ButtonOk_Click"
                Content="{me:LocalizationSource Ok}"
                Command="{Binding SaveCommand}" />
            <ui:Button
                Margin="8"
                Width="80"
                IsCancel="true"
                Click="ButtonCancel_Click"
                Content="{me:LocalizationSource Cancel}" />
        </StackPanel>
    </Grid>
</core:WpfUIPlatformWindow>
