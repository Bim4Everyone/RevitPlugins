# RevitEnumerateBySpline (Нумератор по линии)
Плагин позволяет нумеровать помещения по линии (сплайну).
# Сборка проекта
```
nuke compile --profile RevitEnumerateBySpline
```

# Публикация проекта
```
nuke publish --profile RevitEnumerateBySpline
```
