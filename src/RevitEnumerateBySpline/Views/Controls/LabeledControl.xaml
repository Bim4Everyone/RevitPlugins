<UserControl
    x:Class="RevitEnumerateBySpline.Views.Controls.LabeledControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:converters="clr-namespace:dosymep.WpfCore.Converters;assembly=dosymep.WpfCore"
    x:Name="_this"
    mc:Ignorable="d"
    d:DesignHeight="50"
    d:DesignWidth="300">
    
    <UserControl.Resources>
        <converters:BoolToVisibilityConverter
            x:Key="MainEditControlVisible"
            TrueValue="Visible"
            FalseValue="Collapsed" />
        
        <converters:BoolToObjectConverter
            x:Key="BorderThickness"
            TrueValue="1"
            FalseValue="0" />
    </UserControl.Resources>
    
    
    
    <StackPanel>
        <Border
        VerticalAlignment="Center"
        HorizontalAlignment="Stretch"
        Padding="{DynamicResource CardControlPadding}"
        Background="{DynamicResource CardBackground}"
        BorderBrush="{DynamicResource CardBorderBrush}"
        CornerRadius="{DynamicResource ControlCornerRadius}"
        BorderThickness="{Binding HeaderEditControlBorderVisible, ElementName=_this, 
                Converter={StaticResource BorderThickness}}">
            
        
            <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <ContentControl
                Grid.Column="0"
                Grid.Row="0" 
                Focusable="False"
                FontSize="24"
                VerticalAlignment="Center"
                Margin="{DynamicResource CardControlIconMargin}"
                Foreground="{DynamicResource CardForeground}"
                Content="{Binding Icon, ElementName=_this}" />

            <StackPanel
                Grid.Column="1"
                Grid.Row="0" >
                <ui:TextBlock
                    Margin="0"
                    FontTypography="BodyStrong"
                    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                    Text="{Binding Header, ElementName=_this}"
                    TextWrapping="WrapWithOverflow" />
                <ui:TextBlock
                    Appearance="Secondary"
                    Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                    Text="{Binding Description, ElementName=_this}"
                    TextWrapping="WrapWithOverflow" />
            </StackPanel>

            <ContentControl
                Grid.Column="2"
                Grid.Row="0" 
                Width="500"
                VerticalAlignment="Center"
                Margin="{DynamicResource CardControlContentMargin}"
                Content="{Binding HeaderEditControl, ElementName=_this}" />
            </Grid>
        </Border>
        
        <Border
            VerticalAlignment="Center"
            HorizontalAlignment="Stretch"
            Visibility="{Binding MainEditControlVisible, ElementName=_this, 
                Converter={StaticResource MainEditControlVisible}}"
            Background="{DynamicResource CardBackground}"
            BorderBrush="{DynamicResource CardBorderBrush}"
            CornerRadius="0,0,5,5"
            BorderThickness="1,0,1,1">
            
            <ContentControl
                VerticalAlignment="Top"
                Margin="{DynamicResource CardControlContentMargin}"
                Content="{Binding MainEditControl, ElementName=_this}" />
        </Border>
    </StackPanel>
</UserControl>