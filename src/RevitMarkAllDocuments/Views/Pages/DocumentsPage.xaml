<core:WpfUIPlatformPage
    x:Class="RevitMarkAllDocuments.Views.DocumentsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:vms="clr-namespace:RevitMarkAllDocuments.ViewModels"
    xmlns:me="clr-namespace:dosymep.WpfCore.MarkupExtensions;assembly=dosymep.WpfCore"
    xmlns:core="clr-namespace:dosymep.WpfUI.Core"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    
    ScrollViewer.CanContentScroll="False"
    
    x:ClassModifier="internal">

    <core:WpfUIPlatformPage.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary
                    Source="../Styles/Styles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </core:WpfUIPlatformPage.Resources>

    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <TextBlock
            Grid.Row="0"
            Margin="4,0,0,8"
            Style="{StaticResource HeaderName}"
            Text="{me:LocalizationSource DocumentsPage.SelectDocs}" />
        <Border
            Grid.Row="1"
            Style="{StaticResource CustomBorder}">
            <ui:DataGrid
                AutoGenerateColumns="False"
                x:Name="Documents"
                HeadersVisibility="None"
                CanUserResizeColumns="True"
                Style="{StaticResource CustomResizableDataGridStyle}"
                ItemsSource="{Binding Documents}">
                <DataGrid.Columns>
                    <DataGridCheckBoxColumn
                        Binding="{Binding Path=IsChecked, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}">
                        <DataGridCheckBoxColumn.CellStyle>
                            <Style
                                TargetType="DataGridCell">
                                <EventSetter
                                    Event="CheckBox.Checked"
                                    Handler="CheckBox_Checked" />
                                <EventSetter
                                    Event="CheckBox.Unchecked"
                                    Handler="CheckBox_Unchecked" />
                                <Setter
                                    Property="Background"
                                    Value="Transparent" />
                                <Setter
                                    Property="BorderThickness"
                                    Value="0" />
                            </Style>
                        </DataGridCheckBoxColumn.CellStyle>
                    </DataGridCheckBoxColumn>
                    <DataGridTextColumn
                        Binding="{Binding Path=Name}"
                        Width="400"
                        IsReadOnly="True" />
                    <DataGridTextColumn
                        Binding="{Binding Path=DocumentType}"
                        Width="250"
                        IsReadOnly="True" />
                </DataGrid.Columns>

            </ui:DataGrid>
        </Border>
    </Grid>
</core:WpfUIPlatformPage>
