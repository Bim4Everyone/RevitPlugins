# RevitExportSpecToJson (Экспорт ВОР в JSON)
Плагин предназначен для быстрого экспорта спецификаций в формат JSON.

# Сборка проекта
```
nuke compile --profile RevitExportSpecToJson
```

# Публикация проекта
```
nuke publish --profile RevitExportSpecToJson
```
