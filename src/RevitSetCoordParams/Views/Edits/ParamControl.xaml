<UserControl x:Class="RevitSetCoordParams.Views.Edits.ParamControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Margin="5"
    x:Name="_this"
    mc:Ignorable="d"
    d:DesignHeight="100"
    d:DesignWidth="800">    

    <Border
        VerticalAlignment="Center"
        HorizontalAlignment="Stretch"
        Padding="{DynamicResource CardControlPadding}"
        Background="{DynamicResource CardBackground}"
        BorderBrush="{DynamicResource CardBorderBrush}"
        CornerRadius="{DynamicResource ControlCornerRadius}"
        BorderThickness="{DynamicResource CardControlBorderThemeThickness}">
        
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition />
            </Grid.RowDefinitions>
            
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="100" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>            

            <StackPanel
                Grid.Column="0"
                Grid.Row="0">
                <ui:TextBlock                    
                    FontTypography="BodyStrong"
                    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                    Text="{Binding Header, ElementName=_this}"
                    TextWrapping="WrapWithOverflow" />
                <ui:TextBlock
                    Appearance="Secondary"
                    Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                    Text="{Binding Description, ElementName=_this}"
                    TextWrapping="WrapWithOverflow" />
            </StackPanel>

            <ui:ToggleSwitch                
                Grid.Column="2"
                Grid.Row="0"
                OnContent="{Binding SwitchOnContent, ElementName=_this}"
                OffContent="{Binding SwitchOffContent, ElementName=_this}"
                IsChecked="{Binding IsChecked, ElementName=_this, Mode=TwoWay}"/>

            <StackPanel
                Grid.Column="0"
                Grid.Row="1"
                Margin="0,10,0,0"
                IsEnabled="{Binding IsChecked, ElementName=_this}">
                <ui:TextBlock
                    FontTypography="BodyStrong"
                    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                    Text="{Binding CommonParamHeader, ElementName=_this}"
                    TextWrapping="WrapWithOverflow" />
                <ui:TextBox
                    Margin="0,5,0,0"
                    Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                    Text="{Binding CommonParam, ElementName=_this}"
                    TextWrapping="WrapWithOverflow" />
            </StackPanel>

            <StackPanel
                Grid.Column="2"
                Grid.Row="1"
                Margin="0,10,0,0"
                IsEnabled="{Binding IsChecked, ElementName=_this}">
                <ui:TextBlock
                    FontTypography="BodyStrong"
                    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                    Text="{Binding ElementParamHeader, ElementName=_this}"
                    TextWrapping="WrapWithOverflow" />
                <ui:TextBox
                    Margin="0,5,0,0"
                    Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                    Text="{Binding ElementParam, ElementName=_this}"
                    TextWrapping="WrapWithOverflow" />
            </StackPanel>

        </Grid>
    </Border>
</UserControl>
