<core:WpfUIPlatformWindow
    x:Class="RevitShakeSpecs.Views.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"

    xmlns:base="clr-namespace:dosymep.WPF.Views"
    xmlns:local="clr-namespace:RevitShakeSpecs.Views"
    xmlns:vms="clr-namespace:RevitShakeSpecs.ViewModels"
    xmlns:core="clr-namespace:dosymep.WpfUI.Core"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:me="clr-namespace:dosymep.WpfCore.MarkupExtensions;assembly=dosymep.WpfCore"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"

    mc:Ignorable="d"
    WindowStartupLocation="CenterOwner"
    Height="270" Width="550"
    MinHeight="270" MinWidth="550"    
    d:DataContext="{d:DesignInstance vms:MainViewModel, IsDesignTimeCreatable=False}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition
                Height="Auto" />
            <RowDefinition
                Height="Auto" />
            <RowDefinition
                Height="Auto" />
            <RowDefinition
                Height="Auto" />
            <RowDefinition
                Height="Auto" />
            <RowDefinition
                Height="*" />
        </Grid.RowDefinitions>

        <ui:TitleBar
            Grid.Row="0"
            Title="{me:LocalizationSource MainWindow.Title}" />

        <ui:TextBlock
            Grid.Row="1"
            Margin="10 5"
            Text="{me:LocalizationSource MainWindow.SelectGroupingParam}"
            VerticalAlignment="Center" />

        <ComboBox
            Grid.Row="2"
            Margin="10 0"
            VerticalContentAlignment="Center"
            ItemsSource="{Binding Path=ProjectSectionParamNames}"
            SelectedItem="{Binding Path=SelectedProjectSectionParamName}" >

            <i:Interaction.Triggers>
                <i:EventTrigger
                    EventName="SelectionChanged">
                    <i:InvokeCommandAction
                        Command="{Binding GetProjectSectionsCommand}" />
                </i:EventTrigger>
            </i:Interaction.Triggers>
        </ComboBox>

        <ui:TextBlock
            Grid.Row="3"
            Margin="10 5"
            Text="{me:LocalizationSource MainWindow.SelectDocPackage}"
            VerticalAlignment="Center" />

        <ComboBox
            Grid.Row="4"
            Margin="10 0"
            VerticalContentAlignment="Center"
            ItemsSource="{Binding Path=ProjectSections}"
            SelectedItem="{Binding Path=SelectedProjectSection}">
        </ComboBox>

        <StackPanel
            Grid.Row="5"
            Orientation="Horizontal"
            VerticalAlignment="Bottom"
            HorizontalAlignment="Right">
            <ui:TextBlock
                Margin="10"
                Foreground="Orange"
                VerticalAlignment="Center"
                Text="{Binding Path=ErrorText}" />

            <ui:Button
                Margin="10"
                Width="110"
                Appearance="Info"
                Content="{me:LocalizationSource MainWindow.Option1}"
                Command="{Binding RegenerateCommand}"
                Click="ButtonOk_Click"
                ToolTip="{me:LocalizationSource MainWindow.Option1Tooltip}"
                ToolTipService.InitialShowDelay="1000"
                ToolTipService.ShowDuration="20000"
                ToolTipService.BetweenShowDelay="1" />

            <ui:Button
                Margin="10"
                Width="110"
                Appearance="Info"
                Content="{me:LocalizationSource MainWindow.Option2}"
                Command="{Binding ShakeCommand}"
                Click="ButtonOk_Click"
                ToolTip="{me:LocalizationSource MainWindow.Option2Tooltip}"
                ToolTipService.InitialShowDelay="1000"
                ToolTipService.ShowDuration="20000"
                ToolTipService.BetweenShowDelay="1" />

            <ui:Button
                Margin="10"
                Width="80"
                Content="{me:LocalizationSource MainWindow.ButtonCancel}"
                Click="ButtonCancel_Click" />
        </StackPanel>
    </Grid>
</core:WpfUIPlatformWindow>
