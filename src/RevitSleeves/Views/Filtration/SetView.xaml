<UserControl
    x:Class="RevitSleeves.Views.Filtration.SetView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:RevitSleeves.Views.Filtration"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:vm="clr-namespace:RevitSleeves.ViewModels.Filtration"
    mc:Ignorable="d"
    d:Background="Gray"
    d:DataContext="{d:DesignInstance vm:SetViewModel, IsDesignTimeCreatable=False}">
    <!-- TODO переделать контрол на WPF UI-->
    <UserControl.Resources>
        <Style
            TargetType="ListBoxItem">
            <Setter
                Property="Template">
                <Setter.Value>
                    <ControlTemplate
                        TargetType="ListBoxItem">
                        <ContentPresenter />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style
            TargetType="{x:Type Border}"
            x:Key="border">
            <Setter
                Property="BorderThickness"
                Value="4 1 1 1" />
            <Setter
                Property="Margin"
                Value="2" />
            <Style.Triggers>
                <DataTrigger
                    Binding="{Binding SelectedEvaluator.SetEvaluator.Evaluator}"
                    Value="0">
                    <Setter
                        Property="BorderBrush"
                        Value="Green" />
                </DataTrigger>
                <DataTrigger
                    Binding="{Binding SelectedEvaluator.SetEvaluator.Evaluator}"
                    Value="1">
                    <Setter
                        Property="BorderBrush"
                        Value="MediumBlue" />
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <DataTemplate
            DataType="{x:Type vm:SetViewModel}">
            <Border
                Style="{StaticResource border}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition />
                        <RowDefinition />
                    </Grid.RowDefinitions>

                    <DockPanel
                        Grid.Row="0"
                        LastChildFill="True"
                        HorizontalAlignment="Stretch">
                        <ui:Button
                            DockPanel.Dock="Right"
                            Margin="3"
                            Background="Transparent"
                            BorderThickness="0"
                            Command="{Binding RelativeSource={RelativeSource AncestorType={x:Type ListBox}, Mode=FindAncestor}, Path=DataContext.RemoveSetCommand}"
                            CommandParameter="{Binding RelativeSource={RelativeSource Mode=Self}, Path=DataContext}"
                            Content="{ui:SymbolIcon Delete16}" />
                        <ui:Button
                            DockPanel.Dock="Right"
                            Margin="3"
                            Content="Добавить правило"
                            Command="{Binding AddRuleCommand}" />
                        <ui:Button
                            DockPanel.Dock="Right"
                            Margin="3"
                            Content="Добавить набор"
                            Command="{Binding AddSetCommand}" />
                        <ComboBox
                            DockPanel.Dock="Left"
                            Margin="3"
                            IsEditable="False"
                            DisplayMemberPath="Name"
                            ItemsSource="{Binding Evaluators, UpdateSourceTrigger=PropertyChanged}"
                            SelectedValue="{Binding SelectedEvaluator, UpdateSourceTrigger=PropertyChanged}"
                            HorizontalAlignment="Stretch" />
                    </DockPanel>

                    <ListBox
                        Grid.Row="1"
                        BorderThickness="0"
                        BorderBrush="Transparent"
                        Focusable="False"
                        HorizontalAlignment="Stretch"
                        HorizontalContentAlignment="Stretch"
                        ItemsSource="{Binding Criteria, UpdateSourceTrigger=PropertyChanged}" />

                </Grid>
            </Border>
        </DataTemplate>

        <DataTemplate
            DataType="{x:Type vm:RuleViewModel}">
            <DockPanel
                LastChildFill="True"
                HorizontalAlignment="Stretch">

                <ComboBox
                    DockPanel.Dock="Left"
                    Width="200"
                    Margin="3"
                    IsEditable="False"
                    DisplayMemberPath="ProviderViewModel.DisplayValue"
                    ItemsSource="{Binding CategoryInfo.Parameters}"
                    SelectedValue="{Binding SelectedParameter, UpdateSourceTrigger=PropertyChanged}" />

                <ui:Button
                    DockPanel.Dock="Right"
                    Margin="3"
                    Background="Transparent"
                    BorderBrush="Transparent"
                    BorderThickness="0"
                    Command="{Binding RelativeSource={RelativeSource AncestorType={x:Type ListBox}, Mode=FindAncestor}, Path=DataContext.RemoveRuleCommand}"
                    CommandParameter="{Binding RelativeSource={RelativeSource Mode=Self}, Path=DataContext}"
                    Content="{ui:SymbolIcon Delete16}" />

                <ComboBox
                    DockPanel.Dock="Left"
                    Width="100"
                    Margin="3"
                    IsEditable="False"
                    DisplayMemberPath="Name"
                    ItemsSource="{Binding RuleEvaluators, UpdateSourceTrigger=PropertyChanged}"
                    SelectedValue="{Binding SelectedRuleEvaluator, UpdateSourceTrigger=PropertyChanged}" />

                <Grid
                    DockPanel.Dock="Right">
                    <ComboBox
                        Margin="3"
                        SelectedItem="{Binding SelectedValue, UpdateSourceTrigger=PropertyChanged}"
                        ItemsSource="{Binding Values}"
                        DisplayMemberPath="DisplayValue"
                        Text="{Binding DisplayValue}">
                        <ComboBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <VirtualizingStackPanel
                                    IsVirtualizing="True"
                                    VirtualizingStackPanel.VirtualizationMode="Recycling" />
                            </ItemsPanelTemplate>
                        </ComboBox.ItemsPanel>
                        <ComboBox.ItemTemplate>
                            <DataTemplate
                                DataType="{x:Type vm:ParamValueViewModel}">
                                <TextBlock
                                    Text="{Binding ParamValue.DisplayValue}" />
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                        <ComboBox.Style>
                            <Style
                                TargetType="{x:Type ComboBox}">
                                <Style.Triggers>
                                    <DataTrigger
                                        Binding="{Binding IsValueEditable}"
                                        Value="True">
                                        <Setter
                                            Property="IsEditable"
                                            Value="True"></Setter>
                                    </DataTrigger>
                                    <DataTrigger
                                        Binding="{Binding IsValueEditable}"
                                        Value="False">
                                        <Setter
                                            Property="IsEditable"
                                            Value="False"></Setter>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </ComboBox.Style>
                    </ComboBox>

                </Grid>

            </DockPanel>
        </DataTemplate>

    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition
                Height="Auto" />
            <RowDefinition
                Height="*" />
        </Grid.RowDefinitions>

        <Border
            Grid.Row="0"
            Style="{StaticResource border}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition
                        Height="Auto" />
                    <RowDefinition
                        Height="Auto" />
                </Grid.RowDefinitions>

                <DockPanel
                    Grid.Row="0"
                    HorizontalAlignment="Stretch">
                    <ui:Button
                        DockPanel.Dock="Right"
                        Margin="3"
                        Command="{Binding AddRuleCommand}"
                        Content="Добавить правило" />
                    <ui:Button
                        DockPanel.Dock="Right"
                        Margin="3"
                        Command="{Binding AddSetCommand}"
                        Content="Добавить набор" />
                    <ComboBox
                        DockPanel.Dock="Left"
                        Margin="3"
                        HorizontalAlignment="Stretch"
                        DisplayMemberPath="Name"
                        IsEditable="False"
                        ItemsSource="{Binding Evaluators}"
                        SelectedValue="{Binding SelectedEvaluator, UpdateSourceTrigger=PropertyChanged}" />
                </DockPanel>

                <ListBox
                    Grid.Row="1"
                    x:Name="_lbCriterions"
                    HorizontalAlignment="Stretch"
                    HorizontalContentAlignment="Stretch"
                    Focusable="False"
                    BorderBrush="Transparent"
                    BorderThickness="0"
                    ItemsSource="{Binding Criteria, UpdateSourceTrigger=PropertyChanged}" />

            </Grid>
        </Border>

    </Grid>
</UserControl>
