<UserControl
    x:Class="RevitVolumeModifier.Views.Edits.ParamEditControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:me="clr-namespace:dosymep.WpfCore.MarkupExtensions;assembly=dosymep.WpfCore"
    xmlns:converters="clr-namespace:dosymep.WpfCore.Converters;assembly=dosymep.WpfCore"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    Margin="5"
    x:Name="_this"
    mc:Ignorable="d"
    d:DesignHeight="200"
    d:DesignWidth="800">

    <UserControl.Resources> 
        <converters:BoolToVisibilityConverter
            x:Key="IconVisible"
            TrueValue="Visible"
            FalseValue="Collapsed" />
    </UserControl.Resources>

    <Border
        VerticalAlignment="Center"
        HorizontalAlignment="Stretch"
        Padding="5"
        Background="{DynamicResource CardBackground}"
        BorderBrush="{DynamicResource CardBorderBrush}"
        CornerRadius="{DynamicResource ControlCornerRadius}"
        BorderThickness="{DynamicResource CardControlBorderThemeThickness}">

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition
                    Width="*" />
                <ColumnDefinition
                    Width="30" />
                <ColumnDefinition
                    Width="*" />
            </Grid.ColumnDefinitions>

            <StackPanel
                Grid.Column="0"
                Grid.Row="0"
                VerticalAlignment="Center">
                <ui:TextBlock
                    FontTypography="BodyStrong"
                    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                    Text="{Binding Description, ElementName=_this}"
                    TextWrapping="WrapWithOverflow" />
                <ui:TextBlock
                    Appearance="Secondary"
                    Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                    Text="{Binding DetailDescription, ElementName=_this}"
                    TextWrapping="WrapWithOverflow" />
            </StackPanel>
            
            <ui:SymbolIcon
                Grid.Column="1"
                VerticalAlignment="Center"
                Symbol="Warning24"
                Foreground="OrangeRed"
                FontSize="24"
                ToolTip="{Binding Warning, ElementName=_this}"
                Visibility="{Binding HasWarning, ElementName=_this,
                    Converter={StaticResource IconVisible}}" />
            
            <ui:TextBox
                Grid.Column="2"
                VerticalAlignment="Center"
                Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                Text="{Binding RevitParamName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, ElementName=_this}"
                TextWrapping="WrapWithOverflow" />
        </Grid>
    </Border>
</UserControl>
