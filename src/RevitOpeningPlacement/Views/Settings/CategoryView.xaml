<core:WpfUIPlatformUserControl
    x:Class="RevitOpeningPlacement.Views.Settings.CategoryView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:core="clr-namespace:dosymep.WpfUI.Core"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:me="clr-namespace:dosymep.WpfCore.MarkupExtensions;assembly=dosymep.WpfCore"
    xmlns:vm="clr-namespace:RevitOpeningPlacement.ViewModels.OpeningConfig"
    mc:Ignorable="d"
    d:DesignHeight="450"
    d:DesignWidth="800"
    d:DataContext="{d:DesignInstance vm:MainViewModel, IsDesignTimeCreatable=False}">
    <Grid>
        <ui:ListView
            ItemsSource="{Binding MepCategories}"
            SelectedItem="{Binding SelectedMepCategoryViewModel}">
            <ui:ListView.ItemTemplate>
                <DataTemplate
                    DataType="{x:Type vm:MepCategoryViewModel}">
                    <DockPanel
                        ToolTip="{Binding Name}">
                        <CheckBox
                            IsChecked="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}" />
                        <ui:TextBlock
                            Margin="4 0 0 0"
                            VerticalAlignment="Center"
                            Text="{Binding Name}" />
                    </DockPanel>
                </DataTemplate>
            </ui:ListView.ItemTemplate>
        </ui:ListView>
    </Grid>
</core:WpfUIPlatformUserControl>
