<core:WpfUIPlatformWindow
    x:Class="RevitOpeningPlacement.Views.UnionTaskSettingsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:base="clr-namespace:dosymep.WPF.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="clr-namespace:RevitOpeningPlacement.Views"
    xmlns:vms="clr-namespace:RevitOpeningPlacement.ViewModels.OpeningConfig"
    xmlns:core="clr-namespace:dosymep.WpfUI.Core"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:me="clr-namespace:dosymep.WpfCore.MarkupExtensions;assembly=dosymep.WpfCore"
    WindowStartupLocation="CenterOwner"
    mc:Ignorable="d"
    MinHeight="140"
    MinWidth="350"
    MaxHeight="140"
    MaxWidth="350"
    ResizeMode="NoResize"
    d:DataContext="{d:DesignInstance vms:MainViewModel, IsDesignTimeCreatable=False}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition
                Height="Auto" />
            <RowDefinition
                Height="*" />
            <RowDefinition
                Height="45" />
        </Grid.RowDefinitions>
        <ui:TitleBar
            Grid.Row="0"
            Title="{me:LocalizationSource UnionTaskSettingsView.Title}" />

        <StackPanel
            Grid.Row="1"
            Width="210"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Center">
            <DockPanel
                Margin="0 5"
                HorizontalAlignment="Stretch">
                <ui:ToggleSwitch
                    Content="{me:LocalizationSource UnionTaskSettingsView.RoundSize}"
                    IsChecked="{Binding RoundUnitedTaskSize, UpdateSourceTrigger=PropertyChanged}" />
                <ComboBox
                    Width="70"
                    IsEditable="False"
                    HorizontalAlignment="Right"
                    ToolTip="{me:LocalizationSource UnionTaskSettingsView.RoundValues}"
                    IsEnabled="{Binding RoundUnitedTaskSize}"
                    ItemsSource="{Binding EnabledRoundings}"
                    SelectedItem="{Binding SelectedSizeRoundForUnitedTask, UpdateSourceTrigger=PropertyChanged}" />
            </DockPanel>
            <DockPanel
                HorizontalAlignment="Stretch">
                <ui:ToggleSwitch
                    Content="{me:LocalizationSource UnionTaskSettingsView.RoundElevation}"
                    IsChecked="{Binding RoundUnitedTaskElevation, UpdateSourceTrigger=PropertyChanged}" />
                <ComboBox
                    Width="70"
                    IsEditable="False"
                    HorizontalAlignment="Right"
                    ToolTip="{me:LocalizationSource UnionTaskSettingsView.RoundValues}"
                    IsEnabled="{Binding RoundUnitedTaskElevation}"
                    ItemsSource="{Binding EnabledRoundings}"
                    SelectedItem="{Binding SelectedElevationRoundingForUnitedTask, UpdateSourceTrigger=PropertyChanged}" />
            </DockPanel>
        </StackPanel>

        <DockPanel
            Grid.Row="2"
            HorizontalAlignment="Right"
            VerticalAlignment="Bottom">
            <ui:Button
                Width="80"
                Margin="10"
                Appearance="Info"
                IsDefault="True"
                Click="ButtonOk_Click"
                Content="{me:LocalizationSource Ok}" />
            <ui:Button
                Width="80"
                IsCancel="True"
                Click="ButtonCancel_Click"
                Content="{me:LocalizationSource Cancel}" />
        </DockPanel>
    </Grid>
</core:WpfUIPlatformWindow>
