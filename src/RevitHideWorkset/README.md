# RevitHideWorkset (скрыть РН связей)
Скрытие РН связей из проекта

# Сборка проекта
```
nuke compile --profile RevitHideWorkset
```

# Публикация проекта
```
nuke publish --profile RevitHideWorkset
```
