<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="clr-namespace:RevitSplitMepCurve.ViewModels.Providers"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml">
    <Style
        TargetType="ComboBox"
        x:Key="SymbolComboBox">
        <Setter
            Property="IsEditable"
            Value="True" />
        <Setter
            Property="IsTextSearchEnabled"
            Value="True" />
        <Setter
            Property="IsTextSearchCaseSensitive"
            Value="False" />
        <Setter
            Property="TextSearch.TextPath"
            Value="Name" />
    </Style>

    <DataTemplate DataType="{x:Type vm:PipesProviderViewModel}">
        <Grid Margin="0,4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <ui:TextBlock
                Grid.Column="0"
                VerticalAlignment="Center"
                Text="{Binding RoundSymbol.Label}" />
            <ComboBox
                Grid.Column="1"
                DisplayMemberPath="Name"
                Style="{StaticResource SymbolComboBox}"
                ItemsSource="{Binding RoundSymbol.AvailableItems}"
                SelectedItem="{Binding RoundSymbol.SelectedItem}"
                ScrollViewer.VerticalScrollBarVisibility="Auto" />
        </Grid>
    </DataTemplate>

    <DataTemplate DataType="{x:Type vm:DuctsProviderViewModel}">
        <StackPanel Orientation="Vertical">
            <Grid Margin="0,4,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="200" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <ui:TextBlock
                    Grid.Column="0"
                    VerticalAlignment="Center"
                    Text="{Binding RoundSymbol.Label}" />
                <ComboBox
                    Grid.Column="1"
                    DisplayMemberPath="Name"
                    Style="{StaticResource SymbolComboBox}"
                    ItemsSource="{Binding RoundSymbol.AvailableItems}"
                    SelectedItem="{Binding RoundSymbol.SelectedItem}"
                    ScrollViewer.VerticalScrollBarVisibility="Auto" />
            </Grid>
            <Grid Margin="0,4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="200" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <ui:TextBlock
                    Grid.Column="0"
                    VerticalAlignment="Center"
                    Text="{Binding RectangleSymbol.Label}" />
                <ComboBox
                    Grid.Column="1"
                    DisplayMemberPath="Name"
                    Style="{StaticResource SymbolComboBox}"
                    ItemsSource="{Binding RectangleSymbol.AvailableItems}"
                    SelectedItem="{Binding RectangleSymbol.SelectedItem}"
                    ScrollViewer.VerticalScrollBarVisibility="Auto" />
            </Grid>
        </StackPanel>
    </DataTemplate>

</ResourceDictionary>
