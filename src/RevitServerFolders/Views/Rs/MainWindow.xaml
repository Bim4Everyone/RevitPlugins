<core:WpfUIPlatformWindow
    x:Class="RevitServerFolders.Views.Rs.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:vms="clr-namespace:RevitServerFolders.ViewModels.Rs"
    xmlns:converters="clr-namespace:RevitServerFolders.Converters"
    xmlns:core="clr-namespace:dosymep.WpfUI.Core"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:me="clr-namespace:dosymep.WpfCore.MarkupExtensions;assembly=dosymep.WpfCore"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:pluginBehaviors="clr-namespace:RevitServerFolders.Behaviors"
    mc:Ignorable="d"
    WindowStartupLocation="CenterOwner"
    Height="450"
    Width="800"
    MinHeight="450"
    MinWidth="800"
    d:DataContext="{d:DesignInstance vms:MainViewModel, IsDesignTimeCreatable=False}">

    <i:Interaction.Triggers>
        <i:EventTrigger
            EventName="Loaded">
            <i:InvokeCommandAction
                Command="{Binding LoadViewCommand}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary
                    Source="../../Resources/CustomStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <converters:RsNodeImageConverter
                x:Key="RsNodeImageConverter"
                Empty="{ui:SymbolIcon Symbol=Document20}"
                Server="{ui:SymbolIcon Symbol=Database20}"
                Folder="{ui:SymbolIcon Symbol=Folder20}"
                OpenedFolder="{ui:SymbolIcon Symbol=FolderOpen20}" />
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition
                Height="Auto" />
            <RowDefinition
                Height="Auto" />
            <RowDefinition
                Height="*" />
            <RowDefinition
                Height="Auto" />
        </Grid.RowDefinitions>

        <ui:TitleBar
            Grid.Row="0"
            Title="{me:LocalizationSource RsBrowser.Title}" />

        <DockPanel
            Grid.Row="1"
            Margin="5"
            LastChildFill="False">
            <ui:Button
                ToolTip="{me:LocalizationSource RsBrowser.RefreshNode}"
                Content="{ui:SymbolIcon Symbol=ArrowSync20}"
                Command="{Binding ReloadChildrenCommand}" />
        </DockPanel>

        <Border
            Grid.Row="2"
            Margin="5"
            Style="{StaticResource CustomBorder}">
            <DockPanel
                LastChildFill="True">
                <DockPanel
                    DockPanel.Dock="Top"
                    LastChildFill="True">
                    <Label
                        Width="50"
                        DockPanel.Dock="Right"
                        Content="{me:LocalizationSource RsBrowser.SizeHeader}" />
                    <Label
                        Content="{me:LocalizationSource RsBrowser.NameHeader}" />
                </DockPanel>
                <TreeView
                    ItemsSource="{Binding Items}"
                    SelectedValuePath="{Binding SelectedItem}">
                    <TreeView.ItemContainerStyle>
                        <Style
                            TargetType="TreeViewItem">
                            <Setter
                                Property="pluginBehaviors:ExpandedTreeViewItemsBehavior.ExpandedCommand"
                                Value="{Binding LoadChildrenCommand}" />
                            <Setter
                                Property="pluginBehaviors:ExpandedTreeViewItemsBehavior.ExpandedCommandParameter"
                                Value="{Binding}" />
                        </Style>
                    </TreeView.ItemContainerStyle>
                    <TreeView.ItemTemplate>
                        <HierarchicalDataTemplate
                            ItemsSource="{Binding Children}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition
                                        Width="*" />
                                    <ColumnDefinition
                                        Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <DockPanel
                                    Grid.Column="0">
                                    <Image
                                        Width="20"
                                        Height="20"
                                        Source="{Binding ., Converter={StaticResource RsNodeImageConverter}}" />
                                    <ui:TextBlock
                                        Text="{Binding Name}" />
                                </DockPanel>
                                <ui:TextBlock
                                    HorizontalAlignment="Right"
                                    Grid.Column="1"
                                    Text="{Binding Size}" />
                            </Grid>
                        </HierarchicalDataTemplate>
                    </TreeView.ItemTemplate>
                </TreeView>
            </DockPanel>
        </Border>

        <StackPanel
            Grid.Row="3"
            Orientation="Horizontal"
            HorizontalAlignment="Right">
            <ui:TextBlock
                Margin="10"
                Style="{StaticResource CustomErrorText}"
                Text="{Binding ErrorText}" />

            <ui:Button
                Margin="10"
                Width="80"
                Appearance="Info"
                IsDefault="True"
                Click="ButtonOk_Click"
                Content="{me:LocalizationSource Ok}"
                Command="{Binding AcceptViewCommand}" />

            <ui:Button
                Margin="10"
                Width="80"
                IsCancel="True"
                Click="ButtonCancel_Click"
                Content="{me:LocalizationSource Cancel}" />
        </StackPanel>
    </Grid>
</core:WpfUIPlatformWindow>