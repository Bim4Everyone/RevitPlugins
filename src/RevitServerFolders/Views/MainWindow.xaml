<core:WpfUIPlatformWindow
    x:Class="RevitServerFolders.Views.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:RevitServerFolders.Views"
    xmlns:vms="clr-namespace:RevitServerFolders.ViewModels"
    xmlns:core="clr-namespace:dosymep.WpfUI.Core"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:me="clr-namespace:dosymep.WpfCore.MarkupExtensions;assembly=dosymep.WpfCore"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:wpfbehaviors="clr-namespace:dosymep.WpfCore.Behaviors;assembly=dosymep.WpfCore"
    xmlns:converters="clr-namespace:dosymep.WpfCore.Converters;assembly=dosymep.WpfCore"
    xmlns:dosymepConverters="clr-namespace:dosymep.WPF.Converters"
    xmlns:pluginConverters="clr-namespace:RevitServerFolders.Converters"
    mc:Ignorable="d"
    WindowStartupLocation="CenterOwner"
    Height="450"
    Width="1010"
    MinHeight="450"
    MinWidth="1010"
    d:DataContext="{d:DesignInstance vms:MainViewModel, IsDesignTimeCreatable=False}">

    <i:Interaction.Behaviors>
        <wpfbehaviors:WpfAttachServiceBehavior
            AttachableService="{Binding ProgressDialogFactory}" />
        <wpfbehaviors:WpfAttachServiceBehavior
            AttachableService="{Binding OpenFolderDialogService}" />
    </i:Interaction.Behaviors>
    <i:Interaction.Triggers>
        <i:EventTrigger
            EventName="Loaded">
            <i:InvokeCommandAction
                Command="{Binding LoadViewCommand}" />
            <i:InvokeCommandAction
                Command="{Binding SourceFolderChangedCommand}" />
        </i:EventTrigger>
        <i:EventTrigger
            SourceName="_sourceFolderEdit"
            EventName="TextChanged">
            <i:InvokeCommandAction
                Command="{Binding SourceFolderChangedCommand}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary
                    Source="../Resources/CustomStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <dosymepConverters:InverseBooleanConverter
                x:Key="InverseBooleanConverter" />

            <converters:BoolToVisibilityConverter
                x:Key="BooleanToVisibilityConverter" />

            <ui:SymbolRegular
                x:Key="DocumentIcon">Document20</ui:SymbolRegular>

            <ui:SymbolRegular
                x:Key="FolderIcon">Folder20</ui:SymbolRegular>

            <ui:SymbolRegular
                x:Key="BuildingIcon">Building20</ui:SymbolRegular>

            <pluginConverters:CellImageConverter
                x:Key="CellImageConverter"
                Empty="{StaticResource DocumentIcon}"
                Model="{StaticResource BuildingIcon}"
                Folder="{StaticResource FolderIcon}" />

            <ui:TextBlock
                x:Key="NameHeader"
                Text="{me:LocalizationSource MainWindow.NameHeader}" />
            <ui:TextBlock
                x:Key="FullNameHeader"
                Text="{me:LocalizationSource MainWindow.FullNameHeader}" />
            <ui:TextBlock
                x:Key="TargetsHeader"
                Text="{me:LocalizationSource MainWindow.TargetsHeader}" />

            <Style
                x:Key="CustomDataGridTextDisabledStyle"
                TargetType="TextBlock"
                BasedOn="{StaticResource DefaultDataGridTextElementStyle}">
                <Style.Triggers>
                    <DataTrigger
                        Value="True"
                        Binding="{Binding Path=SkipObject}">
                        <Setter
                            Property="Foreground"
                            Value="{DynamicResource TextFillColorDisabledBrush}" />
                    </DataTrigger>
                </Style.Triggers>
            </Style>

            <Style
                TargetType="DockPanel"
                x:Key="VisibleOnExecutingPanel">
                <Style.Triggers>
                    <DataTrigger
                        Binding="{Binding DataContext.SelectedSettings.OpenFromFoldersCommand.IsExecuting,
                        RelativeSource={RelativeSource AncestorType=Window}}"
                        Value="True">
                        <Setter
                            Property="Visibility"
                            Value="Visible" />
                    </DataTrigger>
                </Style.Triggers>
                <Setter
                    Property="Visibility"
                    Value="Hidden" />
            </Style>
            <Style
                TargetType="Border"
                x:Key="DisabledOnExecutingBorder"
                BasedOn="{StaticResource CustomBorder}">
                <Style.Triggers>
                    <DataTrigger
                        Binding="{Binding DataContext.SelectedSettings.OpenFromFoldersCommand.IsExecuting,
                        RelativeSource={RelativeSource AncestorType=Window}}"
                        Value="True">
                        <Setter
                            Property="IsEnabled"
                            Value="False" />
                    </DataTrigger>
                </Style.Triggers>
                <Setter
                    Property="IsEnabled"
                    Value="True" />
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition
                Height="Auto" />
            <RowDefinition
                Height="Auto" />
            <RowDefinition
                Height="*" />
            <RowDefinition
                Height="Auto" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition
                Width="200"
                MinWidth="200" />
            <ColumnDefinition
                Width="Auto" />
            <ColumnDefinition
                MinWidth="800" />
        </Grid.ColumnDefinitions>

        <ui:TitleBar
            Grid.Row="0"
            Grid.Column="0"
            Grid.ColumnSpan="3"
            Title="{Binding Title}" />

        <DockPanel
            Grid.Row="1"
            Grid.RowSpan="2"
            Grid.Column="0"
            Grid.ColumnSpan="3"
            Style="{StaticResource VisibleOnExecutingPanel}">
            <ui:ProgressRing
                IsIndeterminate="True" />
        </DockPanel>

        <Border
            Grid.Row="1"
            Grid.RowSpan="2"
            Grid.Column="0"
            Margin="10 0 0 0"
            Style="{StaticResource DisabledOnExecutingBorder}">
            <DockPanel
                LastChildFill="True">
                <ui:Button
                    Margin="0 0 0 10"
                    DockPanel.Dock="Top"
                    Content="{ui:SymbolIcon Symbol=Add20}"
                    Command="{Binding AddSettingsCommand}" />
                <ui:DataGrid
                    Grid.Row="5"
                    Margin="5"
                    Style="{StaticResource CustomResizableDataGridStyle}"
                    ItemsSource="{Binding SettingsCollection}"
                    SelectedItem="{Binding SelectedSettings}">
                    <ui:DataGrid.Columns>
                        <DataGridTextColumn
                            Width="Auto"
                            IsReadOnly="True"
                            CanUserReorder="False"
                            CanUserResize="False"
                            CanUserSort="True"
                            Header="№"
                            Binding="{Binding Index}" />
                        <DataGridCheckBoxColumn
                            CanUserReorder="False"
                            CanUserResize="False"
                            CanUserSort="False"
                            Width="Auto"
                            Binding="{Binding IsSelected}">
                            <DataGridCheckBoxColumn.Header>
                                <CheckBox
                                    IsChecked="{Binding DataContext.AllSelected, RelativeSource={RelativeSource AncestorType=ui:DataGrid}, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                            </DataGridCheckBoxColumn.Header>
                        </DataGridCheckBoxColumn>
                        <DataGridTemplateColumn
                            Width="200"
                            Header="{StaticResource TargetsHeader}">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel
                                        Height="50"
                                        Margin="5 2">
                                        <DockPanel
                                            Margin="0 0 0 5">
                                            <ui:TextBlock
                                                Width="55"
                                                Margin="0 0 5 0"
                                                Foreground="{DynamicResource LabelForeground}"
                                                Text="{Binding TargetFromLabel}" />
                                            <Label
                                                Margin="0"
                                                ToolTip="{Binding TargetFolder}"
                                                Content="{Binding TargetFolder}" />
                                        </DockPanel>
                                        <DockPanel>
                                            <ui:TextBlock
                                                Width="55"
                                                Margin="0 0 5 0"
                                                Foreground="{DynamicResource LabelForeground}"
                                                Text="{Binding TargetToLabel}" />
                                            <Label
                                                Margin="0"
                                                ToolTip="{Binding SourceFolder}"
                                                Content="{Binding SourceFolder}" />
                                        </DockPanel>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn
                            CanUserReorder="False"
                            CanUserResize="False"
                            CanUserSort="False"
                            Width="Auto">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <ui:Button
                                        Appearance="Danger"
                                        Content="{ui:SymbolIcon Symbol=Delete20}"
                                        Command="{Binding DataContext.RemoveSettingsCommand,
                        RelativeSource={RelativeSource AncestorType=Window}}"
                                        CommandParameter="{Binding}" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </ui:DataGrid.Columns>
                </ui:DataGrid>
            </DockPanel>
        </Border>

        <GridSplitter
            Grid.Column="1"
            Grid.Row="1"
            Grid.RowSpan="2"
            Width="10"
            Background="{DynamicResource CardBackground}"
            BorderBrush="{DynamicResource CardBorderBrush}"
            BorderThickness="{DynamicResource CardControlBorderThemeThickness}"
            HorizontalAlignment="Center"
            VerticalAlignment="Stretch" />

        <Border
            Margin="0 0 10 5"
            Grid.Row="1"
            Grid.Column="2"
            Style="{StaticResource DisabledOnExecutingBorder}">
            <StackPanel>
                <DockPanel>
                    <ui:TextBlock
                        Width="130"
                        Text="{me:LocalizationSource MainWindow.LabelTarget}" />
                    <ui:ToggleSwitch
                        Margin="5 0"
                        IsChecked="{Binding SelectedSettings.ClearTargetFolder}"
                        Content="{me:LocalizationSource MainWindow.ClearTarget}" />
                    <ui:ToggleSwitch
                        IsChecked="{Binding SelectedSettings.OpenTargetWhenFinish}"
                        Content="{me:LocalizationSource MainWindow.OpenTargetWhenFinish}" />
                </DockPanel>
                <DockPanel
                    Margin="0 5"
                    LastChildFill="True">
                    <ui:Button
                        DockPanel.Dock="Right"
                        Height="36"
                        Margin="5 0 0 0"
                        Content="{ui:SymbolIcon Symbol=Folder20}"
                        Command="{Binding SelectedSettings.OpenFolderDialogCommand}" />
                    <ui:TextBox
                        Text="{Binding SelectedSettings.TargetFolder, UpdateSourceTrigger=PropertyChanged}" />
                </DockPanel>
                <DockPanel
                    Margin="0 5 0 0">
                    <ui:TextBlock
                        Width="130"
                        Text="{me:LocalizationSource MainWindow.LabelSource}" />
                    <ui:ToggleSwitch
                        Margin="5 0"
                        Content="{me:LocalizationSource MainWindow.ExportRooms}"
                        IsChecked="{Binding SelectedSettings.IsExportRooms}"
                        Visibility="{Binding SelectedSettings.IsExportRoomsVisible, 
                    Converter={StaticResource BooleanToVisibilityConverter}}" />
                </DockPanel>
                <DockPanel
                    Margin="0 5 0 0"
                    LastChildFill="True">
                    <ui:Button
                        DockPanel.Dock="Right"
                        Height="36"
                        Margin="5 0 0 0"
                        Content="{ui:SymbolIcon Symbol=Folder20}"
                        IsEnabled="{Binding SelectedSettings.SourceFolderChangedCommand.IsExecuting,
                        Converter={StaticResource InverseBooleanConverter}}"
                        Command="{Binding SelectedSettings.OpenFromFoldersCommand}" />
                    <ui:TextBox
                        x:Name="_sourceFolderEdit"
                        Text="{Binding SelectedSettings.SourceFolder, UpdateSourceTrigger=PropertyChanged}" />
                </DockPanel>
            </StackPanel>
        </Border>

        <Border
            Margin="0 5 10 0"
            Grid.Row="2"
            Grid.Column="2"
            Style="{StaticResource DisabledOnExecutingBorder}">

            <ui:DataGrid
                Grid.Row="5"
                Margin="5"
                RowHeight="33"
                Style="{StaticResource CustomResizableDataGridStyle}"
                CellStyle="{StaticResource CustomDataGridCell}"
                ItemsSource="{Binding SelectedSettings.ModelObjects}"
                SelectedItem="{Binding SelectedSettings.SelectedObject}">

                <ui:DataGrid.Columns>
                    <DataGridCheckBoxColumn
                        CanUserReorder="False"
                        CanUserResize="False"
                        CanUserSort="False"
                        Width="Auto"
                        Binding="{Binding SkipObject, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource InverseBooleanConverter}}">
                        <DataGridCheckBoxColumn.Header>
                            <CheckBox
                                IsChecked="{Binding DataContext.SelectedSettings.SkipAll, RelativeSource={RelativeSource AncestorType=ui:DataGrid}, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource InverseBooleanConverter}}" />
                        </DataGridCheckBoxColumn.Header>
                    </DataGridCheckBoxColumn>
                    <DataGridTemplateColumn
                        CanUserReorder="False"
                        CanUserResize="False"
                        CanUserSort="False"
                        Width="Auto">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <ui:SymbolIcon
                                    Symbol="{Binding ., Converter={StaticResource CellImageConverter}}" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn
                        Width="250"
                        IsReadOnly="True"
                        MinWidth="100"
                        ElementStyle="{StaticResource CustomDataGridTextDisabledStyle}"
                        Binding="{Binding Name}"
                        Header="{StaticResource NameHeader}" />
                    <DataGridTextColumn
                        IsReadOnly="True"
                        MinWidth="100"
                        ElementStyle="{StaticResource CustomDataGridTextDisabledStyle}"
                        Binding="{Binding FullName}"
                        Header="{StaticResource FullNameHeader}" />
                </ui:DataGrid.Columns>
            </ui:DataGrid>
        </Border>

        <StackPanel
            Grid.Row="3"
            Grid.Column="0"
            Grid.ColumnSpan="3"
            Orientation="Horizontal"
            HorizontalAlignment="Right">
            <ui:TextBlock
                Margin="10"
                Style="{StaticResource CustomErrorText}"
                Text="{Binding ErrorText}" />

            <ui:Button
                Margin="10"
                Width="80"
                Appearance="Info"
                IsDefault="True"
                Click="ButtonOk_Click"
                Content="{me:LocalizationSource Ok}"
                Command="{Binding AcceptViewCommand}" />

            <ui:Button
                Margin="10"
                Width="80"
                IsCancel="True"
                Click="ButtonCancel_Click"
                Content="{me:LocalizationSource Cancel}" />
        </StackPanel>
    </Grid>
</core:WpfUIPlatformWindow>