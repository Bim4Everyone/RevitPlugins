<UserControl
    x:Class="RevitLintelsManager.Views.Edits.ComboBoxControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    x:Name="_this"
    mc:Ignorable="d"
    d:DesignHeight="30" d:DesignWidth="300"
    Margin="5">
    

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition
                Width="{Binding WidthTextBlock, ElementName=_this}"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <ui:TextBlock
            Margin="20,0,0,0"
            VerticalAlignment="Center"
            FontTypography="BodyStrong"
            Foreground="{DynamicResource TextFillColorPrimaryBrush}"
            Text="{Binding TextTextBlock, ElementName=_this}"
            TextWrapping="WrapWithOverflow" />
        <ComboBox
            Grid.Column="1"
            ItemsSource="{Binding ItemsSource, ElementName=_this}"
            SelectedValue="{Binding SelectedValue, ElementName=_this}"
            DisplayMemberPath="Name" />
    </Grid>
    
</UserControl>