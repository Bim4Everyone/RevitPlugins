<UserControl
    x:Class="RevitMarkPlacement.Controls.GroupControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:RevitMarkPlacement.Controls"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:converters="clr-namespace:dosymep.WpfCore.Converters;assembly=dosymep.WpfCore"

    x:Name="_this"
    mc:Ignorable="d">

    <UserControl.Resources>
        <converters:StringToVisibilityConverter x:Key="StringToVisibilityConverter" />
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ui:TextBlock
            Grid.Row="0"
            Margin="0,0,0,10"
            FontTypography="BodyStrong"
            Foreground="{DynamicResource TextFillColorPrimaryBrush}"
            Text="{Binding HeaderText, ElementName=_this}"
            Visibility="{Binding HeaderText, ElementName=_this,
                Converter={StaticResource StringToVisibilityConverter}}" />

        <ItemsControl
            Grid.Row="1"
            x:Name="_itemsControl"/>
    </Grid>
</UserControl>