<core:WpfUIPlatformWindow
    x:Class="RevitApartmentPlans.Views.ViewTemplateAdditionWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="clr-namespace:RevitApartmentPlans.Views"
    xmlns:vms="clr-namespace:RevitApartmentPlans.ViewModels"
    xmlns:core="clr-namespace:dosymep.WpfUI.Core"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:me="clr-namespace:dosymep.WpfCore.MarkupExtensions;assembly=dosymep.WpfCore"
    xmlns:converters="clr-namespace:dosymep.WPF.Converters"
    mc:Ignorable="d"
    MinHeight="200"
    MinWidth="450"
    MaxHeight="200"
    MaxWidth="450"
    d:Background="White"
    d:DataContext="{d:DesignInstance vms:ViewTemplateAdditionViewModel, IsDesignTimeCreatable=False}"
    ResizeMode="NoResize"
    WindowStartupLocation="CenterOwner">
    <Window.Resources>
        <converters:RevitViewTypeConverter
            x:Key="RevitViewTypeConverter" />
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition
                Height="Auto" />
            <RowDefinition />
            <RowDefinition
                Height="Auto" />
        </Grid.RowDefinitions>

        <ui:TitleBar
            Grid.Row="0"
            ShowMaximize="False"
            ShowMinimize="False"
            Title="{me:LocalizationSource ViewTemplateAdditionWindow.Title}" />

        <DockPanel
            Grid.Row="1"
            VerticalAlignment="Center"
            HorizontalAlignment="Center">
            <ComboBox
                Width="150"
                Margin="0 0 5 0"
                ItemsSource="{Binding EnabledViewTypes}"
                SelectedItem="{Binding SelectedViewType}"
                IsEditable="False">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock
                            Text="{Binding Converter={StaticResource RevitViewTypeConverter}}" />
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox >
            <ComboBox
                Width="200"
                ItemsSource="{Binding EnabledViewTemplates}"
                SelectedItem="{Binding SelectedViewTemplate}"
                IsTextSearchEnabled="True"
                IsTextSearchCaseSensitive="False"
                ScrollViewer.VerticalScrollBarVisibility="Visible"
                DisplayMemberPath="Name" />
        </DockPanel>

        <StackPanel
            Grid.Row="2"
            Orientation="Horizontal"
            HorizontalAlignment="Right">
            <TextBlock
                Margin="10 0 10 10"
                Foreground="Orange"
                VerticalAlignment="Center"
                Text="{Binding ErrorText}" />

            <ui:Button
                Margin="10 0 10 10"
                Width="80"
                IsDefault="True"
                Appearance="Info"
                Click="ButtonOk_Click"
                Content="{me:LocalizationSource OK}"
                Command="{Binding AcceptViewCommand}" />

            <ui:Button
                Margin="10 0 10 10"
                Width="80"
                IsCancel="True"
                Click="ButtonCancel_Click"
                Content="{me:LocalizationSource Cancel}" />
        </StackPanel>
    </Grid>
</core:WpfUIPlatformWindow>
