# RevitMepTotals (Выгрузить объемы)
Выгружает объемы воздуховодов, труб и изоляции из выбранных проектов без их открытия в Excel.

# Сборка проекта
```
nuke compile --profile RevitMepTotals
```

# Публикация проекта
```
nuke publish --profile RevitMepTotals
```
