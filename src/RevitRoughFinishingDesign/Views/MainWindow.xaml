<base:PlatformWindow
    x:Class="RevitRoughFinishingDesign.Views.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:base="clr-namespace:dosymep.WPF.Views"
    xmlns:local="clr-namespace:RevitRoughFinishingDesign.Views"
    xmlns:vms="clr-namespace:RevitRoughFinishingDesign.ViewModels"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    mc:Ignorable="d"
    WindowStartupLocation="CenterOwner"
    Title="{DynamicResource MainWindow.Title}"
    x:Name="_this"
    Width="600"
    ResizeMode="NoResize"
    SizeToContent="WidthAndHeight"
    d:DataContext="{d:DesignInstance vms:MainViewModel, IsDesignTimeCreatable=False}">

    <i:Interaction.Triggers>
        <i:EventTrigger
            EventName="Loaded">
            <i:InvokeCommandAction
                Command="{Binding LoadViewCommand}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition
                Height="Auto" />
            <RowDefinition
                Height="*" />
            <RowDefinition
                Height="Auto" />
        </Grid.RowDefinitions>
        <ListView
            Grid.Row="1"
            ItemsSource="{Binding Items}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <DockPanel HorizontalAlignment="Center"
                               VerticalAlignment="Center">
                        <!--<ComboBox
                            VerticalAlignment="Center"
                            Height="25"
                            Width="200"
                            VerticalContentAlignment="Center"
                            Margin="5"
                            ItemsSource="{Binding DataContext.WallTypes, ElementName=_this}"
                            SelectedItem="{Binding WallTypeVM}" />-->
                        <TextBlock
                            VerticalAlignment="Center"
                            Width="350"
                            Margin="5"
                            Text="{Binding WallTypeVM}"/>
                        <ComboBox
                            VerticalAlignment="Center"
                            Height="25"
                            Width="250"
                            Margin="5"
                            VerticalContentAlignment="Center"
                            ItemsSource="{Binding DataContext.LineStyles, ElementName=_this}"
                            SelectedItem="{Binding LineStyleVM}" />
                    </DockPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <StackPanel Grid.Row="0" 
                    Orientation="Horizontal" 
                    VerticalAlignment ="Center"
                    HorizontalAlignment="Center">
            <TextBlock
                VerticalAlignment="Center"
                HorizontalAlignment="Center"
                Margin="5"
                Text="Отступ линий, в мм:" />
            <TextBox
                Grid.Row="0"
                Width="200"
                Margin="5"
                VerticalAlignment="Center"
                HorizontalAlignment="Center"
                Text="{Binding LineOffset,
        UpdateSourceTrigger=PropertyChanged}" />
        </StackPanel>
        

        <StackPanel
            Grid.Row="2"
            Height="45"
            Orientation="Horizontal"
            HorizontalAlignment="Right">
            <TextBlock
                Margin="10"
                Foreground="Orange"
                VerticalAlignment="Center"
                Text="{Binding ErrorText}" />

            <Button
                Margin="10"
                Width="80"
                Content="{DynamicResource MainWindow.ButtonOk}"
                IsDefault="True"
                Click="ButtonOk_Click"
                Command="{Binding AcceptViewCommand}" />

            <Button
                Margin="10"
                Width="80"
                Content="{DynamicResource MainWindow.ButtonCancel}"
                IsCancel="True"
                Click="ButtonCancel_Click" />
        </StackPanel>
    </Grid>
</base:PlatformWindow>
