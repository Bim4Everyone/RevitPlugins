<core:WpfUIPlatformUserControl
    x:Class="RevitParamsChecker.Views.Controls.CrudButtonsControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:core="clr-namespace:dosymep.WpfUI.Core"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:me="clr-namespace:dosymep.WpfCore.MarkupExtensions;assembly=dosymep.WpfCore"
    xmlns:local="clr-namespace:RevitParamsChecker.Views.Controls"
    x:Name="_this"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="300"
    x:ClassModifier="internal">
    <core:WpfUIPlatformUserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary
                    Source="../../Resources/CustomStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </core:WpfUIPlatformUserControl.Resources>
    <DockPanel>
        <ui:Button
            ToolTip="{me:LocalizationSource CrudButtons.Tooltip.Add}"
            Command="{Binding AddCommand, ElementName=_this}"
            Content="{ui:SymbolIcon Symbol=Add20}" />
        <ui:Button
            Margin="8 0"
            ToolTip="{me:LocalizationSource CrudButtons.Tooltip.Rename}"
            Command="{Binding RenameCommand, ElementName=_this}"
            CommandParameter="{Binding RenameCommandParameter, ElementName=_this}"
            Content="{ui:SymbolIcon Symbol=Rename20}" />
        <ui:Button
            ToolTip="{me:LocalizationSource CrudButtons.Tooltip.Copy}"
            Command="{Binding CopyCommand, ElementName=_this}"
            CommandParameter="{Binding CopyCommandParameter, ElementName=_this}"
            Content="{ui:SymbolIcon Symbol=Copy20}" />
        <ui:Button
            Appearance="Danger"
            Margin="8 0 0 0"
            ToolTip="{me:LocalizationSource CrudButtons.Tooltip.Remove}"
            Command="{Binding RemoveCommand, ElementName=_this}"
            CommandParameter="{Binding Path=RemoveCommandParameter, ElementName=_this}"
            Content="{ui:SymbolIcon Symbol=Delete20}" />
    </DockPanel>
</core:WpfUIPlatformUserControl>