<core:WpfUIPlatformUserControl
    x:Class="RevitParamsChecker.Views.Controls.EditableListControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:core="clr-namespace:dosymep.WpfUI.Core"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:me="clr-namespace:dosymep.WpfCore.MarkupExtensions;assembly=dosymep.WpfCore"
    xmlns:local="clr-namespace:RevitParamsChecker.Views.Controls"
    mc:Ignorable="d"
    x:Name="_this"
    d:DesignHeight="300"
    d:DesignWidth="300"
    x:ClassModifier="internal">
    <core:WpfUIPlatformUserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary
                    Source="../../Resources/CustomStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </core:WpfUIPlatformUserControl.Resources>
    <Border
        Style="{StaticResource CustomBorder}">
        <DockPanel
            LastChildFill="True">
            <DockPanel
                DockPanel.Dock="Top">
                <ui:Button
                    ToolTip="{me:LocalizationSource EditableList.Tooltip.Add}"
                    Command="{Binding AddItemCommand, ElementName=_this}"
                    Content="{ui:SymbolIcon Symbol=Add20}" />
                <ui:Button
                    Margin="8 0"
                    ToolTip="{me:LocalizationSource EditableList.Tooltip.Rename}"
                    Command="{Binding RenameItemCommand, ElementName=_this}"
                    CommandParameter="{Binding SelectedItem, ElementName=_list}"
                    Content="{ui:SymbolIcon Symbol=Rename20}" />
                <ui:Button
                    ToolTip="{me:LocalizationSource EditableList.Tooltip.Copy}"
                    Command="{Binding CopyItemCommand, ElementName=_this}"
                    CommandParameter="{Binding SelectedItem, ElementName=_list}"
                    Content="{ui:SymbolIcon Symbol=Copy20}" />
                <ui:Button
                    Appearance="Danger"
                    Margin="8 0 0 0"
                    ToolTip="{me:LocalizationSource EditableList.Tooltip.Remove}"
                    Command="{Binding RemoveItemsCommand, ElementName=_this}"
                    CommandParameter="{Binding Path=SelectedItems, ElementName=_list}"
                    Content="{ui:SymbolIcon Symbol=Delete20}" />
            </DockPanel>
            <ui:ListView
                Margin="0 4 0 0"
                SelectionMode="Extended"
                x:Name="_list"
                ItemsSource="{Binding ListItemsSource, ElementName=_this}"
                SelectedItem="{Binding ListSelectedItem, ElementName=_this}">
                <ui:ListView.ItemTemplate>
                    <DataTemplate>
                        <DockPanel
                            Height="20"
                            Margin="10 4"
                            VerticalAlignment="Center">
                            <ui:TextBlock
                                VerticalAlignment="Center"
                                Text="{Binding Name}" />
                        </DockPanel>
                    </DataTemplate>
                </ui:ListView.ItemTemplate>
            </ui:ListView>
        </DockPanel>
    </Border>
</core:WpfUIPlatformUserControl>