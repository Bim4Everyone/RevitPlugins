<core:WpfUIPlatformUserControl
    x:Class="RevitParamsChecker.Views.Results.ReadonlyRuleControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:RevitParamsChecker.Views.Results"
    xmlns:core="clr-namespace:dosymep.WpfUI.Core"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:me="clr-namespace:dosymep.WpfCore.MarkupExtensions;assembly=dosymep.WpfCore"
    xmlns:vms="clr-namespace:RevitParamsChecker.ViewModels.Rules"
    xmlns:rules="clr-namespace:RevitParamsChecker.Views.Rules"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="300"
    d:DataContext="{d:DesignInstance vms:RuleViewModel, IsDesignTimeCreatable=False}"
    x:ClassModifier="internal">
    <core:WpfUIPlatformUserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary
                    Source="../../Resources/CustomStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </core:WpfUIPlatformUserControl.Resources>
    <Border
        Style="{StaticResource CustomBorder}">
        <StackPanel>
            <Label
                FontSize="{DynamicResource ControlContentThemeFontSize}"
                Content="{Binding Name}" />
            <ui:TextBlock
                Margin="0 0 0 4"
                TextWrapping="Wrap"
                MaxWidth="{Binding ActualWidth, ElementName=_paramsSetControl}"
                FontSize="{DynamicResource ControlContentThemeFontSize}"
                Text="{Binding Description}" />
            <rules:ParamsSetUserControl
                IsEnabled="False"
                x:Name="_paramsSetControl"
                DataContext="{Binding  RootSet}" />
        </StackPanel>
    </Border>
</core:WpfUIPlatformUserControl>