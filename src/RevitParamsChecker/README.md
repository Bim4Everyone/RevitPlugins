# RevitParamsChecker (Проверка параметров)

Плагин для проверки значений параметров заданных элементов в заданных документах

# Сборка проекта

```
nuke compile --profile RevitParamsChecker
```

# Публикация проекта

```
nuke publish --profile RevitParamsChecker
```
