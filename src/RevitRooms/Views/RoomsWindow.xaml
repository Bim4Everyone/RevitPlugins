<core:WpfUIPlatformWindow
    x:Class="RevitRooms.Views.RoomsWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:base="clr-namespace:dosymep.WPF.Views"
    xmlns:local="clr-namespace:RevitRooms.Views"
    xmlns:vms="clr-namespace:RevitRooms.ViewModels"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:core="clr-namespace:dosymep.WpfUI.Core"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:me="clr-namespace:dosymep.WpfCore.MarkupExtensions;assembly=dosymep.WpfCore"

    mc:Ignorable="d"
    WindowStartupLocation="CenterOwner"

    Height="750" Width="1000"    
    MinHeight="750" MinWidth="1000"
    
    d:DataContext="{d:DesignInstance vms:RoomsViewModel, IsDesignTimeCreatable=False}">

    <i:Interaction.Triggers>
        <i:EventTrigger
            EventName="Loaded">
            <i:InvokeCommandAction
                Command="{Binding LoadViewCommand}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>

    <core:WpfUIPlatformWindow.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary
                    Source="Styles.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <BooleanToVisibilityConverter
                x:Key="BoolToVis" />
        </ResourceDictionary>
    </core:WpfUIPlatformWindow.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ui:TitleBar
            VerticalAlignment="Center"
            Grid.Row="0"
            Title="{me:LocalizationSource RoomsWindow.Title}"/>
        
        <Border
            Grid.Row="1"
            Margin="14,0,14,4"
            Style="{StaticResource CustomBorder}">
            <ComboBox
                ItemsSource="{Binding RevitViewModels}"
                DisplayMemberPath="Name"
                SelectedItem="{Binding RevitViewModel}" />
        </Border>

        <Border
            Grid.Row="2"
            Margin="14,4"
            Style="{StaticResource CustomBorder}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                
                <CheckBox
                    Grid.Row="0"
                    Grid.Column="0"
                    Margin="0,4"
                    IsChecked="{Binding RevitViewModel.IsFillLevel}">
                    <TextBlock>
                        <Run Text="{me:LocalizationSource RoomsWindow.SetLevel}"/>
                        <Run Text="{Binding LevelParamName, Mode=OneWay}"/>
                    </TextBlock>
                </CheckBox>

                <CheckBox
                    Grid.Row="0"
                    Grid.Column="1"
                    Margin="0,4"
                    Content="{me:LocalizationSource RoomsWindow.CalculateContourArea}"
                    IsChecked="{Binding RevitViewModel.IsSpotCalcArea}" />

                <CheckBox
                    Grid.Row="1"
                    Grid.Column="1"
                    Margin="0,4"
                    Content="{me:LocalizationSource RoomsWindow.CountRooms}"
                    IsChecked="{Binding RevitViewModel.IsCountRooms}" />

                <CheckBox
                    Grid.Row="1"
                    Grid.Column="0"
                    x:Name="_checkArea"
                    Margin="0,4"
                    Content="{me:LocalizationSource RoomsWindow.CheckChanges}"
                    IsChecked="{Binding RevitViewModel.IsCheckRoomsChanges}" />

                <StackPanel
                    Grid.Row="2"
                    Grid.Column="0"
                    Margin="0,4"
                    Orientation="Horizontal"
                    Visibility="{Binding ElementName=_checkArea,Path=IsChecked, Converter={StaticResource BoolToVis}}">
                    <TextBlock
                        Margin="70,0,10,0"
                        VerticalAlignment="Center"
                        Text="{me:LocalizationSource RoomsWindow.CheckAccuracy}" />
                    <ui:TextBox
                        Width="80"
                        Text="{Binding RevitViewModel.RoomAccuracy, UpdateSourceTrigger=PropertyChanged}" />
                    <TextBlock
                        Margin="10,0,0,0"
                        VerticalAlignment="Center"
                        Text="%" />
                </StackPanel>
            </Grid>
        </Border>

        <Border
            Grid.Row="3"
            Margin="14,4"
            Style="{StaticResource CustomBorder}">
            <StackPanel>
            <DockPanel
                    Grid.Row="7"
                    Margin="10,4,0,4">
                    <TextBlock
                        Width="200"
                        VerticalAlignment="Center"
                        Text="{me:LocalizationSource MainWindow.Phase}" />
                    <ComboBox
                        Margin="10,0,0,0"
                        DisplayMemberPath="Name"
                        ItemsSource="{Binding RevitViewModel.Phases}"
                        SelectedValue="{Binding RevitViewModel.Phase}" />
                </DockPanel>

                <DockPanel
                    Grid.Row="8"
                    Margin="10,4,0,4">
                    <TextBlock
                        x:Name="_text"
                        Width="200"
                        VerticalAlignment="Center"
                        Text="{me:LocalizationSource RoomsWindow.Accuracy}" />
                    <ComboBox
                        Margin="10,0,0,0"
                        ItemsSource="{Binding RevitViewModel.RoundAccuracyValues}"
                        SelectedValue="{Binding RevitViewModel.RoundAccuracy}" />
                </DockPanel>
            </StackPanel>
        </Border>

        <Border
            Grid.Row="4"
            Margin="14,4"
            Style="{StaticResource CustomBorder}">
            <ui:DataGrid
                x:Name="Levels"
                AutoGenerateColumns="False"
                CanUserResizeColumns="True"
                Style="{StaticResource CustomResizableDataGridStyle}"
                IsEnabled="{Binding RevitViewModel.IsAllowSelectLevels}"
                ItemsSource="{Binding RevitViewModel.Levels}">
                <DataGrid.Columns>
                    <DataGridCheckBoxColumn
                        Binding="{Binding Path=IsSelected, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}">
                        <DataGridCheckBoxColumn.Header>
                            <TextBlock
                                Text="{me:LocalizationSource MainWindow.HeaderCheck}" />
                        </DataGridCheckBoxColumn.Header>
                        <DataGridCheckBoxColumn.CellStyle>
                            <Style
                                TargetType="DataGridCell">
                                <EventSetter
                                    Event="CheckBox.Checked"
                                    Handler="CheckBox_Checked" />
                                <EventSetter
                                    Event="CheckBox.Unchecked"
                                    Handler="CheckBox_Unchecked" />
                                <Setter
                                    Property="Background"
                                    Value="Transparent" />
                                <Setter
                                    Property="BorderThickness"
                                    Value="0" />
                            </Style>
                        </DataGridCheckBoxColumn.CellStyle>
                    </DataGridCheckBoxColumn>
                    <DataGridTextColumn
                        Binding="{Binding Path=Name}"
                        Width="400"
                        IsReadOnly="True">
                        <DataGridTextColumn.Header>
                            <TextBlock
                                Text="{me:LocalizationSource MainWindow.HeaderLevels}" />
                        </DataGridTextColumn.Header>
                    </DataGridTextColumn>
                    <DataGridTextColumn
                        Binding="{Binding Path=LevelNames}"
                        Width="250"
                        IsReadOnly="True">
                        <DataGridTextColumn.Header>
                            <TextBlock
                                Text="{me:LocalizationSource MainWindow.HeaderFloors}" />
                        </DataGridTextColumn.Header>
                    </DataGridTextColumn>
                </DataGrid.Columns>

            </ui:DataGrid>
        </Border>

        <CheckBox
            Grid.Row="5"
            Margin="14,4,0,14"
            Content="{me:LocalizationSource RoomsWindow.NotShowWarnings}"
            IsChecked="{Binding RevitViewModel.NotShowWarnings}" />

        <StackPanel 
            Grid.Row="5" 
            Orientation="Horizontal"
            Margin="0,4,14,14"
            HorizontalAlignment="Right">
            <TextBlock 
                Margin="8" 
                VerticalAlignment="Center"
                Text="{Binding RevitViewModel.ErrorText}" 
                Foreground="Orange" />

            <ui:Button
                Content="{me:LocalizationSource RoomsWindow.ButtonCalculateAreas}"
                Margin="4,0"
                Appearance="Info"
                Click="ButtonOK_Click"
                Command="{Binding RevitViewModel.CalculateAreasCommand}" />

            <ui:Button
                Content="{me:LocalizationSource RoomsWindow.ButtonCalculateRooms}"
                Margin="4,0" 
                IsDefault="True"
                Appearance="Info"
                Click="ButtonOK_Click" 
                Command="{Binding RevitViewModel.CalculateCommand}" />

            <ui:Button
                Content="{me:LocalizationSource Button.Cancel}" 
                Width="80"
                Margin="4,0,0,0"
                IsCancel="true" 
                Click="ButtonCancel_Click" />
        </StackPanel>
    </Grid>
</core:WpfUIPlatformWindow>