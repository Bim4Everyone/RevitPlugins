<Window x:Class="RevitRooms.Views.InfoElementsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RevitRooms.Views"
        mc:Ignorable="d"
        Title="InfoElementsWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <ListView Grid.Column="0" ItemsSource="{Binding InfoElements}" SelectedItem="{Binding InfoElement}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="#">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Button Command="{Binding Element.ShowElementCommand}">
                                    <Image Width="16" Height="16" Source="{Binding Element.ImageSource}" />
                                    <Button.Resources>
                                        <Style TargetType="Border">
                                            <Setter Property="CornerRadius" Value="5"/>
                                        </Style>
                                    </Button.Resources>
                                </Button>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn DisplayMemberBinding="{Binding Element.ElementId}" Header="ID" Width="100" />
                    <GridViewColumn DisplayMemberBinding="{Binding Element.Name}" Header="Наименование" Width="200" />
                </GridView>
            </ListView.View>
        </ListView>

        <GridSplitter Grid.Column="1" />

        <ListView Grid.Column="2" ItemsSource="{Binding InfoElement.Errors}">
            <ListView.View>
                <GridView>
                    <GridViewColumn DisplayMemberBinding="{Binding}" Header="Описание" Width="500" />
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>
