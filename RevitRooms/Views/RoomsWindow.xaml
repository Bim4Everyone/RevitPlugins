<Window x:Class="RevitRooms.Views.RoomsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Квартирография" Height="550" Width="500">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <ComboBox Grid.Row="0" Margin="10" ItemsSource="{Binding RevitViewModels}" SelectedItem="{Binding RevitViewModel}">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding DisplayData}"/>
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>

        <CheckBox Grid.Row="1" Margin="10" Content="Рассчитать площадь по пятну" />
        <CheckBox x:Name="_checkArea" Grid.Row="2" Margin="10" Content="Проверить площадь помещений на изменения:" />
        <StackPanel Grid.Row="3" Margin="0,5,10,0" Orientation="Horizontal" Visibility="{Binding ElementName=_checkArea,Path=IsChecked, Converter={StaticResource BoolToVis}}" >
            <TextBlock Margin="50,0,10,0" Text="Точность проверки"/>
            <TextBox Width="50"/>
            <TextBlock Margin="10,0,0,0" Text="%"/>
        </StackPanel>

        <StackPanel Grid.Row="4" Margin="10" Orientation="Horizontal">
            <TextBlock Text="Число знаков после запятой:"/>
            <ComboBox Margin="10,0,0,0" Width="120" />
        </StackPanel>

        <ListView Grid.Row="5" Margin="10">
            <ListView.View>
                <GridView>
                    <GridViewColumn DisplayMemberBinding="{Binding Path=IsSelected}" Header="#" Width="20"/>
                    <GridViewColumn DisplayMemberBinding="{Binding Path=Name}" Header="Уровень" Width="380"/>
                    <GridViewColumn DisplayMemberBinding="{Binding Path=Elevation}" Header="Значение" Width="60"/>
                </GridView>
            </ListView.View>
        </ListView>

        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Рассчитать" Height="25" Width="70" Margin="10" IsDefault="True" Click="ButtonOK_Click" Command="{Binding RevitViewModel.SelectElements}"/>
            <Button Content="Отмена" Height="25" Width="70" Margin="10" IsCancel="true" Click="ButtonCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
