<UserControl x:Class="RevitLintelPlacement.Views.GroupedRulesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RevitLintelPlacement.Views"
             xmlns:vm="clr-namespace:RevitLintelPlacement.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance vm:GroupedRuleCollectionViewModel, IsDesignTimeCreatable=False}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <ListBox Grid.Row="0" ItemsSource="{Binding GroupedRules}">
            <ListBox.ItemTemplate>
                <DataTemplate DataType="{x:Type vm:GroupedRuleViewModel}">
                    <StackPanel Orientation="Horizontal">
                        <TextBox Width="200" Text="{Binding Name}"></TextBox>
                        <ListBox Width="200" ItemsSource="{Binding WallTypes}">
                            <ListBox.ItemTemplate>
                                <DataTemplate DataType="{x:Type vm:WallTypeConditionViewModel}">
                                    <CheckBox IsChecked="{Binding IsChecked}" Content="{Binding Name}"></CheckBox>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition/>
                                <RowDefinition/>
                            </Grid.RowDefinitions>
                            <ListBox Grid.Row="0" Width="350" ItemsSource="{Binding Rules}">
                                <ListBox.ItemTemplate>
                                    <DataTemplate DataType="{x:Type vm:ConcreteRuleViewModel}">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBox Width="80" Text="{Binding OpeningWidthCondition.MinWidth}"></TextBox>
                                            <TextBox Width="80" Text="{Binding OpeningWidthCondition.MaxWidth}"></TextBox>
                                            <TextBox Width="80" Text="{Binding LintelLeftOffsetParameter.LeftOffset}"></TextBox>
                                            <ComboBox Width="100" ItemsSource="{Binding LintelTypes}" SelectedItem="{Binding SelectedLintelType}">
                                                <ComboBox.ItemTemplate>
                                                    <DataTemplate>
                                                        <TextBlock Text="{Binding Name}"/>
                                                    </DataTemplate>
                                                </ComboBox.ItemTemplate>
                                            </ComboBox>
                                        </StackPanel>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                            <Button Grid.Row="1" Width="30" Command="{Binding AddRuleCommand}">+</Button>
                        </Grid>
                        
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <StackPanel Grid.Row="1">
            <Button Width="30" Command="{Binding AddGroupedRuleCommand}">+</Button>
        </StackPanel>
    </Grid>
</UserControl>
