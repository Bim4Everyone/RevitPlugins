<Window x:Class="RevitCopyStandarts.CancellableProgressWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RevitCopyStandarts"
             mc:Ignorable="d" 
            Title="Загрузка ..."
        Height="110" Width="300" 
        ShowInTaskbar="False" 
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize" 
        WindowStyle="SingleBorderWindow"
        FontFamily="Segoe UI"
        >
    <Grid Margin="10">
        <TextBlock x:Name="TextLabel" Text="Пожалуйста подождите ..." TextTrimming="CharacterEllipsis" />
        <TextBlock x:Name="_operation" Margin="0 18 0 0" Text="Выполнение операции ..." FontStyle="Italic" TextTrimming="CharacterEllipsis" />
        <ProgressBar x:Name="ProgressBar" Margin="0 38 74 0" IsIndeterminate="True" />
        <Button x:Name="_btCancel" Margin="0 38 0 0" Width="70" Content="Отмена" HorizontalAlignment="Right" Click="_btCancel_Click" />
    </Grid>
</Window>
